# Status — jednym spojrzeniem

> Zwięzły status bieżący (CLAUDE.md §Workflow pkt 2). Pełna historia: `git log`, GitHub Issues/milestones, `agent/lessons.md`, `Project Plan/*`.
> Przepisany 2026-06-13 (poprzednie 2066 linii append-only logu, m.in. epiki NUI/UI/RBAC — w historii gita).

## 2026-07-14: MARATON „testy-całości pt.2" — 14 ticketów UI/UX (#2565–#2578) wszystkie merged + live-smoke proof
- **Kontekst:** operator rozpisał docx `docs/Tests/calosciowe - od 8072026/testy-calosci - pt 2.docx` na 14 ticketów i autoryzował marathon bez pytań. Każdy = własny branch+PR+CI+merge+live-smoke+proof w issue-close comment. Poprzedziła seria zmian home/produkty (#2562/#2564/#2580) — nowy brand-orange `#ff4f00` (biały tekst, a11y rozluźniona przez `.exclude('.bg-cta')`), merge Column Views+Presety, „Dodaj" w topbarze.
- **✅ DOSTARCZONE (14/14, każdy merged + proof):**
  - **#2565** — rename „Katalogi PDF" → „Web To Print".
  - **#2566** — edycja istniejącego katalogu (EditCatalogWizard, GET prefill, PATCH config; regeneracja osobno).
  - **#2567** — „Zastosuj filtr" na `/catalogs-pdf/new` (StepScope licznik `useCatalogSearch` „N produktów trafi do katalogu").
  - **#2568** — typy katalogu „grid" + „cennik" (BE renderery CPDF-P6-01/02 już były; wizard udostępnia kafle; preview grid/pricelist→200). PR #2591.
  - **#2569** — branding: wgraj logo z Multimediów (`AssetLogoPicker` Sheet, `onSelect(previewUrl)`).
  - **#2570** — mapowanie pól przez `AttributePicker` zamiast wpisywania kodu (ariaLabel prop).
  - **#2571** — rename „Archetyp" → trafniejsza polska nazwa.
  - **#2572** — fix migotania ikon assetów przy wejściu w katalog (`placeholderData: keepPreviousData`).
  - **#2573** — investigation `/settings/api-tokens`: funkcjonalny → zostaje (bez zmian kodu, wyjaśnienie w issue).
  - **#2574** (PR #2592) — przeniesienie edytora definicji workflow ze Settings do huba `/workflow/definitions` + CTA „Definicje przepływu" na WorkflowPage; redirecty starych `/settings/workflow*`; usunięty wpis z settings-nav.
  - **#2575** — nagłówek `/workflow` „Workspace" → „Workflow".
  - **#2576** — API-configurator domyślnie otwiera zakładkę „Połączenia".
  - **#2577** — usunięty nieużywany „Podgląd" na detalu kanału.
  - **#2578** (PR #2593) — usunięcie wyboru ikon/kolorów z 4 powierzchni (kreator ObjectType, tworzenie kategorii, detal ObjectType, modal Smart Preset); defaulty wysyłane cicho, kontrakty API bez zmian. Kluczowe: `icon` w categories/new **nigdy nie był persystowany** (capture-only), preset icon default `ICON_CHOICES[0]` spełnia wymóg BE.
- **Gotchas → lessons:** (1) rename współdzielonej etykiety `nav.*` łamie specy z case-insensitive regexem starej etykiety (`/dashboard|pulpit/i`) — grep literalny nie łapie; grep case-insensitive PL+EN przed pushem. (2) brand-orange `#ff4f00` biały tekst = 3.29:1 (sub-AA) — świadomy wyjątek operatora, `.exclude('.bg-cta')` w każdym axe-specu. (3) `page.request`/apiLogin pada lokalnie na DNS `pim.localhost` (Node) → smoke tylko browser-flow (loginAsAdmin form). (4) typecheck admin OOM w kontenerze → `NODE_OPTIONS=--max-old-space-size=4096`. (5) picker bez data-testid → asercja braku przez tekst etykiety scoped regexem.

## 2026-07-13: API-filters follow-up (#2549/#2550) + 3 bug-fixy po smoke operatora (Bug1/Bug2/Bug3)
- **Kontekst:** kontynuacja wątku filtrów API (po #2527/#2530 scoping live-API) + operator zgłosił 3 problemy po smoke-teście. Każdy = własny branch+PR+CI+merge+live-smoke+proof.
- **✅ #2549 (PR #2551) — outbound-sync filter „Z PIM":** `SyncBinding.outboundFilter` (FilterDsl JSONB, migracja `Version20260713100000` idempotentna) scopuje które produkty PIM WYSYŁA do kanału. Nowy `OutboundRecordReader` seam (Export impl kompiluje DSL przez `FilterDslResolver::toCountSql`), `SyncBindingProcessor`/Input/serializer, FE `OutboundFilterSection` (reużywa `AdvancedFilterPanel`, ukryty dla inbound). Live-proof: POST binding+filter→201, persist round-trip, DELETE→204.
- **✅ #2550 (PR #2552) — live podgląd profilu w builderze:** `POST /api/profiles/preview` (draft-config-via-query, bez zapisu) zwraca realne obiekty scoped wg filtrów+ObjectType i zrzutowane do allow-listy atrybutów. Nowy Catalog Contracts seam `ObjectSamplePort` (impl w Catalog Application; ApiConfigurator za Contracts per deptrac). FE `ProfilePreviewPanel` „Testuj". Live-proof: count=3 published produkty zrzutowane do `name`.
- **✅ Bug1 #2553 (PR #2554) — FE odporność zapisu roli:** endpoint `PATCH /api/roles/{id}` DZIAŁA (200 live); defekt był FE — catch mapował KAŻDY status≠{400,403,409-dup} na ślepy `error_generic`, ukrywając prawdziwy błąd (np. FrankenPHP dev-cache HTML-fatal). Fix: `httpErrorDetail(error)` w fallbacku + `baseSaved` rozróżnia częściową porażkę PATCH→PUT + pierwszy e2e na zapis roli.
- **✅ Bug2 #2555 (PR #2556) — konfigurowalny akceptant end-to-end:** `WORKFLOW_CUSTOM_DEFINITIONS` ON domyślnie (`.env`+`docker-compose`) + seed `reviewer@demo.localhost` (rola `approver`). Pipeline działał; luka = nikt nie miał roli reviewera → review taski widoczne tylko dla Ownera. **NIE seeduję włączonej definicji produktowej** (wyciekała do `wfl-p5-03`/`wfl-p6-02`) — reviewer-persona przez fallback `approver`. Live-proof: configured→catalog_manager ORAZ fallback→approver.
- **➖ Bug3 (zignorowany per operator):** akcja „Zmień status" w toolbarze grida DZIAŁA na main (naprawiona #2494 po tym jak #2483 wrzucił ją do martwego komponentu) — pokazuje się po zaznaczeniu ≥1 wiersza na `/products`, gated `products.bulk_operations`. Prawdopodobnie stary build/brak selekcji.
- **✅ #2558 (PR #2559) — gate submitu na wymagane pola + tłumaczenie tooltipa:** operator zauważył, że pusty produkt wchodził do recenzji i zakleszczał się (bramka completeness była tylko na `approve`/`publish`, nie na `submit_for_review`). Fix: `CompletenessGateGuard` blokuje `submit_for_review` gdy `ObjectType.completeness_rules.required` ma niewypełnione pola (blocker `missing_required`). Blockery wzbogacone o parametry (`min_completeness_pct`, `missing_required`) → discovery serializuje → FE tooltip przez i18n `workflow.blocker.{code}` (pl+en) zamiast surowego angielskiego. **4 specy workflow** uzupełniają wymagane pola przed submitem. Live-proof: empty→409, complete→200.
- **Gotchas → lessons:** (1) seed WŁĄCZONEJ definicji workflow w współdzielonych fixtures wycieka do product-driving e2e (analogia „asset jako wyspa"). (2) flip globalnej flagi: grep WSZYSTKIE testy na asercję wartości (`MeEndpointTest` assertFalse przeoczony przez blast-radius agenta). (3) deptrac Tooling NIE zawiera Workflow (w odróżnieniu od Catalog/Identity/Channel/Asset); lokalny cache maskował (pokazał violation ale „Errors 0"), CI `--no-cache` autorytatywny. (4) walidacja AppFixtures na throwaway DB (schema:create+fixtures:load jako owner `pim` BYPASSRLS) łapie fixture-load fail bez wipe'u dev DB. (5) 2 z 3 „bugów" to nie-defekty — adversarial live-verify PRZED claim „to nie bug".

## 2026-07-12: Workflow redesign — MARATON (konfigurowalny akceptant + przeprojektowany UI, 6 ticketów, wszystkie merged + smoke)
- **Kontekst:** operator po smoke-teście WFL (3 bug-fixy #2491/#2493/#2495) zaprojektował pełny redesign sekcji Workflow (7 ekranów) wokół rdzenia: **konfigurowalny akceptant** — kto dostaje task do skrzynki, per ObjectType, ustawiany w UI zamiast zahardkodowanej roli `approver`. Plan: `~/.claude/plans/graceful-gathering-scott.md`. Marathon: każdy ticket = własny branch+PR+CI+merge+live-smoke+proof.
- **✅ DOSTARCZONE (6/6, każdy merged + live-smoke proof na pim.localhost):**
  - **T1 #2514 (#2513)** — BE silnik routingu: VO `Workflow\Contracts\TaskAssignee` (XOR rola/user), `EditorialWorkflowProvider::reviewerFor(objectTypeId)`, pole `workflow_definitions.reviewer` JSONB (migracja `Version20260712100000`, idempotentna), walidacja istnienia roli/usera+XOR, `WorkflowTaskAutomation` czyta reviewerFor z fallbackiem `approver`; `mine` superset (posiadacze `approve_reject` widzą review/request_unpublish niezależnie od assignee).
  - **T2 #2516 (#2515)** — FE strona „Ustawienia przepływu" (CTA w topbarze huba, `/workflow/settings`): legenda ról, karta flagi, selektor per built-in ObjectType, diagram stanów, **picker akceptanta**, reguły przejść. `directory-api.ts` (useQuery, nie jsonFetch+useEffect).
  - **T3 #2518 (#2517)** — FE+BE kolejka „Do przeglądu": filtr kind (taby), badge kind, avatar+imię+czas zgłaszającego. Nowy seam `Identity\Contracts\Directory\UserDirectoryInterface` (batch id→imię, imię z e-maila).
  - **T4 #2520 (#2519)** — FE+BE karty zadań: współdzielony `WorkflowTaskCard` (badge typu, termin, tytuł+SKU+kind, komentarz, zgłaszający, opcjonalny assignee, action slot). Nowy seam `Catalog\Contracts\Query\ObjectSummaryPort` (batch id→{title,sku,kind}).
  - **T5 #2522 (#2521)** — FE+BE kontrolka na karcie produktu: discovery zwraca `reviewer` → hint „Po zgłoszeniu zadanie trafi do: <rola|osoba> · zmień w Ustawieniach przepływu →"; panel Historia pokazuje `actor_name`. Smoke: flaga OFF→`approver`, flaga ON+def(role=marketing)→`marketing`.
  - **T6 #2524 (#2523)** — FE widget „Moje zadania" na Pulpicie = siatka `WorkflowTaskCard` + filtr-taby + inline Zatwierdź/Odrzuć (review) / Popraw / Rozpatrz (deep-link). Smoke: inline approve→published, task zamknięty.
- **Gotchas maratonu (→ lessons):** (1) full-page axe na karcie produktu łapie `link-in-text-block` — inline link musi mieć `underline` (nie tylko kolor). (2) Motyw remapuje `zinc` na blue-gray: `text-zinc-400`=#7f8ea9=3.3:1 **fail WCAG AA** → `text-zinc-500`=#5b6b87=5.4:1 (latentny bug WorkflowTaskCard z T4, ujawniony pierwszym axe-scanem renderowanej karty). (3) e2e piszące taski roli-akceptanta muszą sprzątać (finally cancel) — współdzielona CI DB, leaked open task zanieczyszcza axe-scan innych speców. (4) Python urllib nie rozwiązuje `pim.localhost` (jak Node) — smoke przez curl. (5) reużycie seamów: UserDirectory (T3) w T4/T5, WorkflowTaskCard (T4) w T6 — jedno źródło designu.

## 2026-07-12: Epik GRID — MARATON (dopracowanie grida per ObjectType: column manager + custom columns + sort + inline edit + saved views)
- **Backlog:** `Project Plan/feature-grid-tickets.md` (23 tickety GRID-P{faza}-{nn}, #2385–#2407, milestone'y #66–#73, label epik-GRID). Strategia PR: 23 tickety → 13 PR-ów (sekcja „Strategia PR" — powód: quality-php na paths-ignore = pełne bramki na każdym PR z kodem).
- **✅ DOSTARCZONE i zmergowane (11 ticketów, każdy live-smoke proof na pim.localhost):**
  - **M1** (#2497/#2499) — dynamiczne kolumny z list-schema: `useGridColumns` model, `resolveGridColumns` (RBAC-safe merge overrides), rejestr cell-rendererów per typ atrybutu, oba widoki (grid+Excel) z ViewColumnSeeds; fallback przy braku schemy.
  - **M2** (#2501) — column manager (show/hide + drag-reorder @dnd-kit + reset), header resize (clamp 60–640, live width + commit na pointerup), sticky identyfikator, density compact/normal. Wszystko persist localStorage per OT.
  - **ADR-0028** (#2502) — strategia sortu: Postgres JSONB path (benchmark 50k: p95 12–25ms bez indeksu, 20–40× zapasu; Meili odrzucony operacyjnie), NULLS LAST + tie-breaker id, LIMIT/OFFSET dla sortowanych. `docs/perf/grid-attribute-sort-benchmark.md`.
  - **M3** (#2503) — `?full=1`: dowolny atrybut ObjectType jako kolumna (281 na demo), default→hidden (manager odkrywa), chip AttributeGroup; RBAC 3-state w obu trybach.
  - **M4-BE** (#2505) — SavedView config: `SavedViewConfigValidator` (unknown key/typ → 400 RFC7807), default per (tenant,object_type_id,user_id), system view (user_id NULL) → PATCH/DELETE 403; owner-scoped przez CurrentUserProvider.
  - **P5 sort** (#2507) — `AttributeOrderFilter` `?order[attribute.{code}]` wg ADR-0028 (cast per typ, jednolity 400 dla unknown/unsortable/restricted), DQL JSONB_PATH_TEXT/NUMERIC, OrderById + pola systemowe, klikalne nagłówki + URL-persist. Fix: list-url-seed strip sort.
  - **P6-01** (#2506) — flaga `editable` per kolumna (canEditAttribute + typ inline-editowalny; media/relation/multiselect/localizable → false).
  - **P6-02** (#2508) — typed inline editors w Excel view (date/number/boolean/select-dropdown z labelkami), commit PATCH atrybutu + optimistic overlay + rollback.
  - **M4-FE** (#2509) — round-trip config przez saved views (filters+columns+sort+density+variants+page_size); reloadToken refetch; fix pre-existing resource mismatch (rail 'products' vs save 'product').
  - **P6-03** (#2510 merged) — typed TSV paste + coercion (select label→code) + skip report toast; `coerceExcelValue` współdzielony z single-cell commit.
- **⏸ ODŁOŻONE do świeżego kontekstu (świadomy quality-stop, EPIK MARATHON RULE — decyzja bliska architektonicznej):**
  - **M7 grid-export (#2403 P7-01 + #2404 P7-02)** — endpoint `POST /api/objects/grid-export` reużywający ExportBuilder/ColumnResolver/ValueSerializer + async job + MinIO + przycisk FE. Duża integracja silnika Export (model sesji/async) — wymaga dokładnego studium, żeby nie dostarczyć zepsutego eksportu.
  - **M8-P8-01 wirtualizacja wierszy (#2405)** — restrukturyzacja render grida (page-scroll → kontener stałej wysokości) zderza się ze sticky columns (P2-02) + variant tree + selekcją (świeżo dostarczone). Paginacja i tak cappuje 200 wierszy — potrzeba umiarkowana. Ryzyko regresji > wartość przy tej długości sesji.
  - **M8-P8-02 journey E2E (#2406)** + **P8-03 closeout+screencast (#2407)** — po M7 i P8-01.
- **Gotchas maratonu → sekcje niżej + memory `grid-epic-backlog`:** biome-ignore w JSX = 1 linia; inline @var ginie przez cs-fixer (użyj typowanego helpera); PR-head desync po force-pushu → close+nowy PR; stale FrankenPHP worker → cache:clear+restart przed live-E2E; toast paste fire'uje przed async PATCH (waitForResponse w E2E); paste wymaga focusu tabeli (stopEditing restore).

## 2026-07-11 (wieczór): 3 bug-fixy po smoke operatora WFL

## 2026-07-11 (wieczór): 3 bug-fixy po smoke operatora WFL (#2491/#2493/#2495 — wszystkie merged + closed z proofem)
- **Kontekst:** operator manualnie smoke-testował workflow po zamknięciu epiku; zgłosił 3 niezależne problemy → 3 osobne issues/PR-y (SKILL-BUG-FIX-TICKET, plan `~/.claude/plans/graceful-gathering-scott.md`).
- **#2491 (PR #2492) — edytor roli 500:** schema drift `role_attribute_permissions` (composite PK bez `id`; migracja `CREATE TABLE IF NOT EXISTS` z `id` była no-opem) → `GET/PUT /api/roles/{id}/attribute-permissions` 500 → mylący toast „Operacja na roli nie powiodła się". Fix: defensywna migracja `Version20260711150000` (ADD id + swap PK, guard na `information_schema`). CI zielone bo testowa DB z mappingów. Smoke: GET/PUT→200, dev DB zmigrowana.
- **#2495 (PR #2496) — „Moje zadania" puste:** taski review/request_unpublish przypisane roli `approver`; `mine` matchował tylko dosłowne członkostwo → admin (super_admin, ma `workflow.approve_reject`, nie approver) nie widział ich, choć fan-out notyfikacji (po permissionie) działał. Fix: `mine` poszerzone o wirtualne członkostwo approver dla posiadaczy `workflow.approve_reject`; `REVIEWER_ROLE`+permission przeniesione do `ObjectEditorialWorkflow` (SoT). Smoke: admin 4× review, marketing 0.
- **#2493 (PR #2494) — brak „Zmień status" na gridzie:** WFL-P3-04 dodał przycisk do `BulkActionsToolbar` (0 importów, martwy po migracji na `UniversalListPage`+`BulkBar`). Fix: wpięcie `onOpenChangeStatus` do BulkBar (product-only) + mount `BulkChangeStatusDialog` w ULV, usunięty martwy toolbar. E2E przepisany na Meili-independent (intercept endpointu — grid jest Meili-backed, świeże produkty nieindeksowane w CI). Smoke: bulk 2 drafty→review.
- **Lekcje → lessons.md:** `CREATE TABLE IF NOT EXISTS` jako vector schema drift (parity migracje↔mapping = kandydat na CI gate); „shipped do martwego komponentu" (grep importów przed dodaniem akcji); mine-audience spójne z fan-outem; Meili-backed grid → E2E przez intercept.
- **User-facing uwaga:** produkt operatora `WFL-TASK2-1783750287` zarchiwizowany (`archived`) — wymaga `restore`→draft z detalu by wrócił do obiegu.

## 2026-07-11: ✅ EPIK WFL ZAMKNIĘTY — Workflow edytorski (28 ticketów #2409–#2436, M0–M6, milestone'y #74–#80)
- **Co:** pełny workflow edytorski dla PIM: symfony/workflow state machine `object_editorial` (draft→review→published→archived, marking na `objects.status`), nowy BC `src/Workflow/` (ADR-0029), guardy transition→permission z danych RBAC, log przejść + procedural endpoints (`/api/objects/{id}/workflow*`), completeness gate na publish, bulk change_status przez maszynę, eventy domenowe + Mercure, powiadomienia in-app (pakiet D), UI: status control + historia na detalu, review queue, edit-lock published + request-unpublish, bulk transitions z grida, my-tasks inbox + widget, encja tasks + automatyka, definicje per tenant w DB + form-based builder (Settings→Workflow) za flagą `WORKFLOW_CUSTOM_DEFINITIONS` (default OFF, provider→statyczny YAML). Marathon 2026-07-10/11, każdy PR: rebase → CI → squash-merge → live-smoke proof w komentarzu issue (CLOSED MEANS CLOSED).
- **Merged PR-y (23):** przygotowanie #2437/#2438/#2453 + ADR #2471 · pakiety A #2472, B #2473, C #2475, D #2479, E #2480, F #2484, G #2486 (definicje+provider, migracja `Version20260711130000`) · P1-01 #2474, P1-04 #2476, P1-05 #2477, P2-01 #2478 · UI: P3-02 #2481, P3-03 #2482, P3-04 #2483, P4-03 #2485, P5-03 #2487 (builder + `feature_flags` na `/api/auth/me`) · testy: P6-01 #2488 (security pass), P6-02 #2489 (E2E dwurolowe + axe) · docs P6-03 #2490.
- **Bug produkcyjny wykryty przez E2E P6-02 (fix w #2489):** sugar path produktów odrzucał userów PRD-only — `ProductVoter` nie mapował uppercase `CREATE`/`READ` (fallback do legacy `object.write`/`object.read`); detal admina ładuje przez poly-kind `GET /api/objects/{id}`. Fix kind-safe: `CREATE_PRODUCT`/`READ_PRODUCT` na operacjach produktowych + instance-only alias `READ` (abstain na class-string — bez eskalacji cross-kind, wzorzec GOLIVE #2129); pinowane `ProductCreatePermissionApiTest`.
- **Świadome odejścia:** self-approve dozwolony w MVP (pin w `WorkflowSecurityApiTest`, docs/workflow.md §7) · builder form-based, nie canvas (decyzja z backlogu) · pakiety PR A–G zamiast 1 ticket=1 PR (decyzja operatora 2026-07-10, 20 PR-ów zamiast 27) · `?object=` → `object_id` w teście skorygowane w P6-03.
- **Następny krok:** kolejny epik wg priorytetów operatora (kandydaci: follow-upy GRID/CPDF, GOLIVE Blok C hosting-gated).

## 2026-07-10: Epik AICG W TOKU — Generowanie treści AI (opisy + SEO), marathon #2325–#2346
- **Sub-faza / epik:** AICG (22 tickety, M0–M6, milestone'y #59–#65, label `epik-AICG`). Backlog SoT: `Project Plan/feature-aicg-tickets.md`; plan: `Project Plan/UI/feature-ai-content-generation.md` (decyzje A–E zatwierdzone 2026-07-08). Tryb: marathon przez cały epik, jeden ticket = jeden PR.
- **✅ AICG-P0-01 #2325 (PR #2443):** ADR przyjęty jako **ADR-0030** (nie 0028 z backlogu — 0028 zarezerwował równolegle GRID, 0029 WFL; wolny numer = pliki ∪ rezerwacje backlogowe). `docs/adr/0030-ai-content-generation-tools.md`: toole treści `kind=Write` w istniejącym ToolRegistry, wyjątek „agent-native" (silnik=LLM), ContentRecipe+BrandVoiceProfile w `src/Agent/` (removability), grounding kontraktowany + `insufficient_grounding`, zero auto-zapisu, SEO w przepisie nie schemacie, defaultModel+BYOK. Backlog + issue przenumerowane.
- **✅ AICG-P0-02 #2326 (PR #2444):** `provenance_meta` + opcjonalne `source_attributes`/`recipe_id` — spec `docs/api/jsonb-schemas.md §5` + projekcja `AttributesIndexedRebuilder::globalSlot` (pass-through gdy well-typed, malformed dropped, gate na `agent_run_id`) + unit test 6 case'ów. **M0 komplet.**
- **⚠️ Hotfix czerwonego main (PR #2445):** PR #2440 (CPDF benchmark) wszedł z czerwonym deptrac (`Tooling` bez warstw `Export_Catalog_*` po splicie #2362) — 12 violations zamaskowanych przez docs-only merge'e; fix = ruleset Tooling + lessons (deptrac `--no-cache`, nie mergować z czerwonym checkiem).
- **✅ AICG-P1-01 #2327 (PR #2446):** encja `ContentRecipe` + migracja `content_recipes` (RLS FORCE, UNIQUE(tenant_id,code)) + guardy (format∈{plain,html}); smoke RLS psql-proof (A=1, B=0).
- **✅ AICG-P1-02 #2328 (PR #2447):** encja `BrandVoiceProfile` + migracja (RLS FORCE + partial unique is_default per tenant); smoke: izolacja A=1/B=0, druga default → ERROR unique. Lekcja: testowa baza z mappingów ORM (nie migracji) — partial indexes re-wydawane w teście.
- **✅ AICG-P1-03 #2329 (PR #2448):** CRUD API Platform obu bytów + moduł RBAC `settings.ai_content` (voter w Agent BC), built-in read-only + clone route, transakcyjny swap defaultu, OpenAPI snapshot; live smoke: 201/403 viewer/clone/swap.
- **✅ AICG-P1-04 #2330 (PR #2449):** seeder built-in przepisów + default głosu, komenda `pim:agent:seed-content-defaults`, fix RLS GUC w konsoli. **M1 KOMPLET.**
- **✅ AICG-P2-01 #2331 (PR #2450):** `ObjectFactsPort` (seam Catalog/Contracts, read-only fakty + overlay locale/channel + sibling locales, provenance stripped) + `ContentGroundingService` (∩ recipe.sourceAttributes, zawężenie publication profile). Gotcha: port bez runtime-konsumenta inline'owany przez kontener → w kernel teście konstrukcja wprost.
- **✅ AICG-P2-02 #2332 (PR #2451, SEC):** `GroundingGate`+`GroundingVerdict` — failing-first w historii; verdict jako structured tool_result; progi z przepisu (`constraints.grounding.{min_facts,required}`, default min_facts=1).
- **✅ AICG-P2-03 #2333 → M3–M5 komplet** (PR-y do #2461): `AgentSystemPromptBuilder` sekcje content, materializer tekstowy `ContentValueMaterializer` (P3-01, PendingChanges scoped), `GenerateProductDescriptionTool` (P3-02, flagowy agent-native) + `GenerateSeoTextTool` (P4-02, SEO w przepisie), `SeoRulesValidator` (P4-01), FE: Ask AI inline (P5-01/02), bulk modal (P5-03 #2341), Settings→AI recipes+voice (P5-04/05 #2342/#2343 bundle).
- **✅ M6 — DOMKNIĘCIE EPIKU (2026-07-10):**
  - **P6-01 #2344 `translate_value`** (PR #2463, merged): trzeci wariant grounded — fakt=wartość@source_locale, output@target_locale, recipe-less, `insufficient_grounding` gdy brak źródła. Live smoke PL→EN (HTML zachowany 1:1) + negatywny (brak wartości → skip). ✅ closed z proofem.
  - **P6-02 #2345 `[SEC]` red-team** (PR #2464, merged): `PromptInjectionRedTeamTest` +3 scenariusze tooli treści (injection nie przekierowuje targetu · grounding code-driven nie wycieka sekretu · sfałszowany batch/wyciek promptu → tylko pending), 6/6. Świadome odejście: potwierdził istniejące zabezpieczenia (brak luki, brak zmiany produkcyjnej). ✅ closed z proofem.
  - **P6-03 #2346 perf** (PR #2465, w CI): `ContentCostEstimator` + `POST /api/agent/content/cost-preview` + `POST /api/agent/content/bulk-generate` (dedykowana ścieżka: `BulkGenerateContentHandler extends AbstractBatchHandler`, batch 200 + `clear()`, reuse tooli 1:1, jeden batch approval) + day-cap gate 2-stopniowy (429). Modal zdjął cap 8 + client-estymatę. Live smoke: preview 200, bulk 2 prod → 202 → awaiting_approval, reject → 0 zapisu.
- **Decyzja operatora (2026-07-10, czat):** selektywne bundle PR-ów dla par producer–consumer: P4-01+P4-02, P5-01+P5-02, P5-04+P5-05 (~3 rundy CI mniej); [SEC] i granice BC bez zmian — osobno.
- **Ostatnie 3 akcje:** (1) merge #2463/#2464 + close #2344/#2345 z proofem, (2) P6-03 backend+FE+3 test-suity (12 testów) + live smoke bulk, (3) rebase P5b (#2462) po konflikcie locales.
- **Następny krok:** CI zielone #2462 (P5b) + #2465 (P6-03) → merge → close #2342/#2343/#2346 z proofem → **EPIK AICG ZAMKNIĘTY (22/22)** → podsumowanie.
- **Blokery:** brak.

## 2026-07-10: ✅ EPIK CPDF ZAMKNIĘTY — Katalogi PDF (27/27 ticketów, M0–M6, #2282–#2308)
- **Co:** pełny web-to-print dla PIM: `CatalogProfile`/`CatalogRun` + silnik szablonów Twig (3 archetypy: sheet / pricelist / grid z okładką+spisem) + port `PdfRenderer` (Dompdf in-process default, Gotenberg sidecar opt-in) + async z progresem Mercure + delivery cache-and-serve z tokenem URL + pełne UI (hub KPI/karty/akcje, kreator 6-krok, live preview, menu gate RBAC). Marathon 2026-07-09/10, każdy ticket: branch → PR → CI → squash-merge → **live smoke proof w komencie zamknięcia** (CLOSED MEANS CLOSED).
- **Finał M5 (UI):** #2382 Mercure live progress (proof: Playwright na żywym stacku łapie subskrypcję EventSource `catalogs/{id}/runs`, progress → terminal → refresh karty), #2383 menu gate (`exports.view_own|view_all` anyOf; persona negatywna = modeler bez `exports.*` → brak wpisu + Forbidden403 na deep-linkach).
- **M6 (archetypy + Gotenberg + decyzja A + parity):**
  - **P6-01 pricelist** (PR #2384): wielostronicowa tabela, `<thead>` table-header-group powtarzany per strona, fixed footer z `counter(page)`. Live: 294 produkty → 12 stron / 595 ms.
  - **P6-02 grid** (PR #2411): okładka → spis treści (kotwice → 294 realne `/Link` w PDF) → siatka `batch(3)`; `target-counter` (numery stron w spisie) gejtowany Twig-flagą `premium` — degradacja Dompdf. Live: 294 → 16 stron / 852 ms.
  - **P6-03 GotenbergRenderer** (PR #2439): drugi adapter portu (Chromium sidecar, multipart, retry 429/5xx backoff 2^n wzorcem Shopify), `CatalogPdfRendererFactory` (`CATALOG_PDF_RENDERER=gotenberg`+`GOTENBERG_URL`, każda inna kombinacja → cichy fallback Dompdf), compose profile `gotenberg`. Live proof na realnym sidecarze.
  - **P6-04 benchmark + cap** (PR #2440): `pim:catalog:benchmark` (kalka EXP-04) na realnych danych — grid@500 produktów = **150.5 MB peak / 1.8 s** (alert 256 MB dopiero ~1200+). **Decyzja A rozstrzygnięta: cap, nie chunk→merge**; `PdfRenderer::supportsLargeDocuments()` (Dompdf false/Gotenberg true), `CATALOG_PDF_MAX_ITEMS=500`, przekroczenie → `CatalogTooLargeException` z komunikatem „włącz Gotenberg" na runie (live-proof z capem 50). Telemetria peak-memory per run w logu regeneratora.
  - **P6-05 parity** (PR #2441): guard „bezpiecznego CSS" (`CatalogTemplateCssGuardTest` — zakaz flex/grid/`var(--`/sticky; premium paged-media tylko w `{% if premium %}`) + snapshot parity Dompdf↔Gotenberg (sheet strony identyczne, pricelist ±1, premium grid Gotenberg-only; cross-engine warunkowo — 4/4 na żywym sidecarze). Dokumentacja w ADR-0027.
- **Świadome odejścia (jednoliniowe, każde w PR body):** wizard FE pokazuje pricelist/grid jako „Wkrótce" (StepMapping sheet-only; archetypy w pełni dostępne przez API — odblokowanie = slot-aware mapping step, kandydat na follow-up); renderer per-profil (`catalog_profiles.renderer` auto/dompdf/gotenberg) pozostaje hookiem — selekcja env-level; numery stron w spisie grida na Dompdf niemożliwe z definicji (target-counter → Gotenberg); zaawansowane formatowanie walut w pricelist — hook per backlog.
- **Env-fixy z marathonu:** dysk VM Dockera 100% (Gotenberg 500 „no space left") → prune ~3.6 GB; zawieszony Playwright runner na CI → cancel+rerun; PHPStan w kontenerze ginie z exit 137 za 1. razem (retry przechodzi) — przez to do P6-03 wszedł 1 realny błąd typów, wyłapany i naprawiony w P6-04.
- **Następny krok:** epik GRID (backlog gotowy, #2385–#2407) — start od GRID-P1-01; ewentualny follow-up CPDF: slot-aware StepMapping odblokowujący pricelist/grid w kreatorze.

## 2026-07-11: Epik GRID — M1 KOMPLETNY (P1-01..04, PR #2497 + #2499 merged)
- **PR 1 (#2497, squash `8ab1f753`)** — GRID-P1-01+P1-02 (lib-only): moduł `apps/admin/src/lib/grid/` — `GridColumn` model + pure `resolveGridColumns` (RBAC-safe: klucz spoza schemy dropowany), hook `useGridColumns` (localStorage per OT + settery pod M2), `extractGridCellValue` (envelope ADR-0019, odporny na garbage) + `GridAttributeCell` (renderery per typ, labelki opcji batch z cache 5 min). Issues #2385/#2386 closed z proofem vitest.
- **PR 2 (#2499, squash `52f9d664`)** — GRID-P1-03+P1-04: oba widoki list (grid + Excel) budują kolumny z modelu. ViewColumnSeeds dla kolumn widokowych (kategorie/sync/cena/fallback nazwy/oś wariantu); `status`/`updatedAt` domyślnie ukryte (parity); `has_multimedia` steruje slotem miniatury; Excel przez adapter `toExcelColumns`/`toExcelRow` (atrybutowe read-only do P6-02, TSV przez projekcje `attr:{code}`). E2E `2387-grid-dynamic-columns.spec.ts` zielony na CI (świeży custom OT + show_in_list + wartość w obu widokach). Issues #2387/#2388 closed z proofem CI-E2E.
- **CI iteracje PR 2:** max-lines guard (512>500 → ekstrakcja products-grid-row.tsx) · regresja labelki SKU (spec 1423) · brakujący junction bulk-attach w e2e setup — lekcje w lessons.md sekcja „GRID M1".
- **Manual smoke:** wymaga wciągnięcia main do serwowanego drzewa (dev stack serwuje working copy operatora) — do wykonania przy najbliższym przełączeniu.
- **Następny krok:** PR 3 = M2 column manager (GRID-P2-01+02+03, #2389–#2391); równolegle można postawić GRID-P5-01 (#2397, ADR-0028 + benchmark — osobny PR z zasady).

## 2026-07-09: Epik GRID — backlog utworzony (plan zaakceptowany, implementacja niezaczęta)
- **Co:** rozpisanie zaakceptowanego planu „Dopracowanie grida per ObjectType" (zarządzanie kolumnami + custom columns; benchmark Akeneo/Plytix/PIMcore) na wykonalny backlog. Decyzje operatora: custom column = dowolny atrybut ObjectType (bez silnika formuł); w zakresie sort po atrybutach + inline edit (Excel view) + eksport „co widzę"; poza zakresem przełącznik locale/channel nad gridem.
- **Deliverables:** backlog **`Project Plan/feature-grid-tickets.md`** = 23 tickety (~135–210h), prefix `GRID`, format `GRID-P{faza}-{nn}`, 8 milestone'ów M1–M8 (GitHub #66–#73), label `epik-GRID`, **Issues #2385–#2407**. ADR-0028 (attribute sort strategy, Postgres JSONB vs Meilisearch + benchmark 50k) finalizowany w GRID-P5-01.
- **Punkt wyjścia (eksploracja 2026-07-09):** backend ~70% gotowy — `list-schema` (ULV-03 #984) zwraca kolumny per ObjectType z RBAC 3-state, lista niesie pełne `attributesIndexed`, `SavedView.config` JSONB z `object_type_id`; frontend tego nie konsumuje (kolumny hardcoded w `universal-list-page.tsx`). **Finding:** ULV-07 #989 (dynamic columns) CLOSED bez artefaktów w kodzie — scope re-delivered w M1/M2/M4.
- **Kolejność:** M1 fundament FE (P1-01 blocker) → M2 manager → M3 full katalog; BE (P3-01, P4-01, P5-01/02, P7-01) równolegle; ścieżka krytyczna P1-01→P1-02→P1-04→P6-02 oraz P5-01→P5-02→P6-02.
- **Następny krok:** start od GRID-P1-01 (#2385) + równolegle GRID-P5-01 (#2397, ADR+benchmark — blocker M5/M6).

## 2026-07-08: bug-fixy po teście bloczka agenta (#2277 + #2279, oba merged + live-smoke)
- **Kontekst:** operator testował bloczek agenta (#2246) — „dodaj atrybut test z bloczka bez grupy"; agent utworzył, operator zatwierdził, ale atrybut „nie dodał się". Diagnoza baza→API→UI: atrybut **powstał** (run done+commit, w bazie), ale był (a) niewidoczny na liście i (b) niepodpięty do niczego. Dwa osobne bugi, dwa PR-y.
- **#1 (#2278 `13562126`) — lista atrybutów gubiła >200:** `list.tsx` pobiera `pagination:{mode:'off'}` (konwencja 44 list w apce), a kolekcja `Attribute.xml` miała `paginationItemsPerPage="200"` (klient nie może wyłączyć paginacji) → przy 289 atrybutach najnowszy w ogonie ~89, nigdy nie pobierany; badge „289" vs nagłówek „200 w bibliotece". Fix: cap 200→1000 + FE sort najnowsze-pierwsze (UUIDv7 `id` desc, zero zmian API). Test: `AttributesCrudApiTest` case >200→wszystkie + Playwright `2277-*` (newest-first). **Live smoke:** API zwraca 289, `test_z_bloczka` pierwszym wierszem, wyszukiwarka znajduje.
- **#2 (#2280 `f02b0e36`) — agent nie ostrzegał o niepodpiętym atrybucie:** `CreateUpdateAttributeTool` tworzy bez `object_type_attributes` i bez grupy → biblioteczny, niewidoczny na produktach. Fix: gdy brak grup, `note`/`description` instruują model, by jawnie ostrzegł (+ pole `attached_to_groups`), bez auto-podpięcia. Test: `CreateUpdateAttributeToolTest` (bez grup→ostrzeżenie+flag false; z grupami→brak). **Live smoke (Haiku BYOK):** agent odpowiedział „⚠️ …utworzony wyłącznie w BIBLIOTECE – nie będzie widoczny na produktach dopóki nie podłączysz w Modelowaniu… mogę Ci go przypisać do grupy".
- **Świadome odejścia:** prawdziwa server-side pagination + server-search dla >~1000 atrybutów (dziś domena „200+", cap 1000 wystarcza) i N+1 `/usage`+`/options` per wiersz — osobny perf follow-up; domyślne podpięcie atrybutu do ObjectType przez agenta — osobna decyzja UX.
- **Lekcja → lessons.md:** e2e vs live-stack (worktree nie jest serwowany — kopiuj plik do main tree by zwalidować spec albo licz na CI); `paginationItemsPerPage` nie trafia do OpenAPI snapshotu; `gh pr merge --delete-branch` w worktree woła „main already checked out" ale zdalny merge przechodzi.

## 2026-07-08: EPIK DASH — uinteraktywnienie bloczków Pulpitu (11 ticketów DASH-01…11, #2249–#2275, wszystkie merged)
- **Co:** wszystkie bloczki dashboardu (poza agentem z #2246) przechodzą z pixel-perfect mocka VIEW-13 na live data. `mocks.ts` usunięty w całości. Plan: `~/.claude/plans/wykonaj-plan-uinteraktywnienia-bloczku-w-witty-hejlsberg.md`, brief `Project Plan/UI/dashboard-brief-makiety.md`, backlog odhaczony w `dashboard-do-oprogramowania.md`.
- **Architektura (ADR-0026):** nowy cienki read-only kontekst `src/Dashboard` (Deptrac: `Dashboard → {Catalog,Channel,Identity}_Contracts + Shared`); dane BC bez seamu czytane raw DBAL-em z jawnym `tenant_id` (precedens `SqlCompletenessReport`, RLS backstop). Wystawia `GET /api/dashboard/{summary,activity,top-edited,alerts}` + `POST …/alerts/{fp}/ack`.
- **Decyzje operatora (2026-07-04):** alerty = **agregator on-the-fly + tabela ack-ów** po fingerprincie (NIE encja Alert; zero seamów zdarzeniowych w 4 BC; okno czasowe zamiast TTL); delty = **dzienne snapshoty** `dashboard_snapshots` (uczciwy null zamiast fabrykowanego trendu do narośnięcia historii).
- **Tickety (każdy: issue+branch+PR+CI+merge+live-smoke proof w komentarzu):**
  - **DASH-01** #2250 — deep-link filtrów listy produktów (`searchStringToDsl`, fundament drill-downów) + fix katalogu atrybutów panelu (kolumny systemowe).
  - **DASH-02** #2252 — kafle Produkty/Gotowe live + wszystkie kafle i progi legendy jako drill-downy.
  - **DASH-03** #2254 — `ChannelCompletenessPort` + SQL po `objects.completeness->per_channel` (Catalog Contracts).
  - **DASH-04** #2256 — ADR-0026 + kontekst Dashboard + `GET /api/dashboard/summary` (1 request zamiast 9 sond FE).
  - **DASH-05** #2258 — `dashboard_snapshots` (FORCE RLS + grant pim_app) + job `pim:dashboard:snapshot` 03:45 UTC + delty na horyzontach 30d/7d.
  - **DASH-06** #2260 — FE w całości na summary: Średnia kompletność + Kompletność wg kanału live, realna delta produktów, degradacja „—".
  - **DASH-07** #2262 — `GET /api/dashboard/activity` + `/top-edited` (seria gap-fill z `objects.created_at`+`audit_logs`, ranking z hydratacją; indeks `audit_logs(tenant_id,created_at)`).
  - **DASH-08** #2264 — TeamActivityCard live: wykres z API, range 7/30/90 w URL, wiersze top-edited = `Link` do `/products/:id`.
  - **DASH-09** #2266 — `AlertFeedAggregator` (5 typów: sync/import/feed/webhook/completeness_drop), fingerprinty deterministyczne, per-typ RBAC, `dashboard_alert_acks`, `openAlerts` w summary.
  - **DASH-10** #2274 — ActionCenter live: CTA per typ (deep-linki do źródeł), tytuły składane z params przez i18n (parytet pl/en), optimistic ack, empty state „Wszystko działa ✓", „Pokaż wszystkie". **Usuwa ostatni mock.**
  - **DASH-11** #2275 — sweep: docblocki „static mock" → „live data", nazwa spec, parytet i18n zweryfikowany (72=72).
- **Świadome odejścia (udokumentowane w PR-ach):** per-channel filtr listy produktów odroczony (wymaga pól per-channel w Meili); top-edited bez etykiety kategorii (relacja poza zakresem); „modified" = proxy per request HTTP (undercount bulk/import, `dh-auditor` nie śledzi CatalogObject); badge trendu ukrywany zamiast mockowego; e2e asertuje search-request nie response-status (CI bez indeksu Meili → 503-degraded). Poza zakresem epiku: Operacje w toku (§5-E), Dystrybucja kanałowa (§5-G), System/Backup (§5-H), konfigurowalne KPI, Recent agent activity — kandydaci na kolejny epik.
- **Stan: ✅ EPIK ZAMKNIĘTY.** Wszystkie 11 PR-ów merged, 11 issue closed z live-smoke proofami. Gates per ticket: PHPStan max / deptrac / cs-fixer / PHPUnit (ApiTestCase na realnym Postgresie) / vitest / Playwright / spec-drift OpenAPI. **Live smoke po całości** (`https://pim.localhost`): 4 endpointy dashboardu HTTP 200, agregator wykrył realny historyczny alert (partial import IdoSell, 4 błędy) bez seedowania. Dashboard suites BE: 20/20 (103 asercje); vitest FE: 137/137.

## 2026-07-04 (wieczór): #2246 — żywy bloczek agenta na dashboardzie + dynamiczne quick actions (PR #2247)
- **Co:** hero dashboardu (VIEW-13) przestaje być mockiem: submit → realny run (`surface: 'dashboard'`, nowy case enum, bez migracji) → handoff do AgentChatSheet (`OPEN_AGENT_CHAT_EVENT`) → pełna pętla + approval w Skrzynce. **Auto-dziedziczenie:** wolny tekst z definicji (ToolRegistry/tag `agent.tool`); chipy przez `GET /api/agent/capabilities` z rejestru (RBAC+autonomy per user) — narzędzie deklaruje chip opcjonalnym `ProvidesQuickActionInterface` (5 narzędzi na start), ⌘K konsumuje ten sam endpoint (usunięte hardcoded `AGENT_SUGGESTIONS`). Licznik „42 zaakceptowanych zmian" usunięty (decyzja operatora); chipy SEO/tłumaczenia wypadły (brak narzędzi — wrócą automatycznie gdy powstaną).
- **Decyzje bezpieczeństwa capabilities:** zawsze 200 `{enabled, reason, actions}` (discovery, `#[NoPermissionRequired]` z uzasadnieniem); **permission przed guardem BYOK** (user bez `agent.bulk_actions` nie pozna stanu klucza tenanta — finding z adwersaryjnego review, naprawiony przed pushem); autonomy off ⇒ `enabled:true, actions:[]` (spójne z ⌘K).
- **Testy:** `AgentCapabilitiesApiTest` 7 case'ów (RBAC-filtering na personach legacy catalog_manager+PRD marketing, maskowanie BYOK, autonomy off) + `surface:'dashboard'`→201 + Playwright `2246-*.spec.ts` (5 testów: submit/chipy/409-adopcja/⌘K-parity/dim+CTA) + `view-13` zaktualizowany (placeholder, stub capabilities, licznik out). Lokalnie: PHPUnit Agent 143/143, PHPStan 0, cs-fixer 0, Deptrac 0, vitest 120/120, build + removability build. OpenAPI +25 linii.
- **Stan: ✅ ZAMKNIĘTE.** PR #2247 merged (squash `21172fd0`, CI 13/13; po drodze 2 poprawki: regen snapshotu OpenAPI po dodaniu `NoPermissionRequired` + stub-order w specu 1422). **Live smoke PASSED** (`https://pim.localhost`): capabilities 200 z 5 chipami z rejestru → hero submit `POST /api/agent/runs` 201 (`surface:dashboard`) → realny tool call `completeness_report` na `claude-haiku-4-5` ($0.026) → chat sheet „Planuję…"→„Czekam na odpowiedź", konsola czysta; ⌘K te same sugestie na żywo. #2246 closed z proofem w komentarzu. Worktree posprzątany.

## 2026-07-04 (sesja follow-up + load + agent live): GOLIVE — 5 ticketów zamkniętych, agent live smoke, 4 nowe findingi
- **Dyrektywa:** wszystko lokalnie bez hostingu (zaakceptowane). W trakcie sesji **operator wprowadził klucz Anthropic BYOK** → odblokowany #2136.
- **✅ Zamknięte z live-proofem (5):**
  - **#2225** meiliLiteral (PR #2228) — kanoniczny escaper `MeiliFilterLiteral`, oba writery delegują. Smoke: injection-value w filtrze → 200/0 hits (jeden literał).
  - **#2222** alerty up==0 (PR #2230) — blackbox exporter + grupa `pim-dependencies` (`DependencyDown`/`ApiDown`/`BlackboxProberDown`). Live: stop Meili → alert pending→firing(2m)→cleared.
  - **#2221** MinIO outage→503 — **DWUCZĘŚCIOWY**: PR #2229 (listener FilesystemException→503) był NIEKOMPLETNY (live smoke: stop-minio → 30s hang → 500 HTML bo Guzzle bez timeoutu). Dokończony **PR #2232** (`connect_timeout:5s`+`timeout:20s` na S3 client). Live: stop-minio → 503 problem+json w 0.12s. **Lekcja: smoke wykrył niedorobiony merged fix — dlatego #2221 był OPEN.**
  - **#2128** load session (PR #2235 raport `docs/perf/golive-load-session-2026-07.md`) — seed 50k (peak 71 MiB płaski) + reindex 50118 docs + k6 p95 + endurance 15min. p95@5VU: bulk 71/search 92/preflight 95 ms ✅, **GET /api/products 481 ms ❌ (algorytmiczne→#2234)**. Endurance pamięć płaska 40-90 MiB (brak wycieku). Purge→demo 118.
- **✅ #2136 agent LIVE smoke (klucz BYOK operatora, model Opus):** headline AC **udowodnione** — realny tool call `aggregate_count`→„100 produktów", cost $0.066/run, cost-tracking działa. **Ticket OPEN** (approval-flow write czeka na #2237; limit-$20 nie symulowany na żywo — realny koszt).
  - **KRYTYCZNY bug znaleziony+fix (PR #2236):** agent async loop routuje `AgentRunMessage`→transport `import` (worker), pierwsza ścieżka `IdempotencyMiddleware INSERT processed_messages` jako `pim_app` pod RLS → **dead-letter 42501 permission denied** (tabela zniknęła + brak grantu sprzed #2177 + self-heal CREATE jako pim_app niemożliwy w privilege-split). Migracja idempotentna CREATE IF NOT EXISTS + GRANT. Niewidoczne bez live agenta — **dokładnie po to jest #2136**.
- **Nowe findingi (OPEN):** **#2231** reconcile OOM na 50k (`toIterable` buforuje result-set + per-obiekt findBy) · **#2233** worker trzyma stale S3 connection po recovery MinIO → 503 aż do restartu · **#2234** GET /api/products p95 481ms@5VU (serializacja listy) · **#2237** agent search po `sku` (niefilterowalny w Meili) → mylnie „degraded" zamiast błędu filtra, blokuje write-grounding.
- **#2140 RODO** — pełny audyt as-is + rekomendacja podziału na issue (export=bezpieczny slice, erasure+dokumenty=decyzja operatora). OPEN partial.
- **Pozostało lokalnie:** #2141 UAT IdoSell (demo czyste, procedura gotowa: structural groups→attrs→products, 7/60/1026). Blok C hosting-gated: #2138/#2139 (część)/#2140 (część).
- **Lekcje → lessons.md:** agent transport=`import` (nie async/sync); `processed_messages` privilege-split (klasa #2177); S3 client bez timeoutu wisi do PHP max_execution; merged fix ≠ works (live smoke obowiązkowy); k6 20VU=kontencja lokalna vs 5VU=algorytm; polskie znaki w inline curl/python wywracają zsh → plik payloadu.

## 2026-07-04 (TOR 2): GOLIVE Blok B wykonany lokalnie — security-heavy, 2 realne findingi naprawione
- **Dyrektywa:** cały TOR 2 lokalnie (bez hostingu, zaakceptowane), nie przerywać. Baseline #2127 = `6201c7f`.
- **✅ Zamknięte z proofami (harnessy w `scripts/security/` + `scripts/smoke/` + `scripts/chaos/`, raporty w `docs/security/` + `docs/audit/2026-07-handover/`):**
  - **#2130** regresja 5 CRITICAL audytu 2026-06 → **5/5 PASS** (AUD-001 Mercure 401 / AUD-002 pim_app false/false + FORCE RLS 55 tabel / AUD-004 Meili filter-key 400 / AUD-005 tracked-secrets clean / AUD-007 token_dev_only env-gated).
  - **#2134** izolacja tenantów, 7 powierzchni → **7/7 PASS** (REST read/write/collection, Meili search, Postgres RLS, asset binary, feed public URL — zero cross-tenant).
  - **#2135** smoke krytycznych ścieżek → **11/11 PASS** (pełny CRUD produktu + auth/import/export/feedy/APIC/RBAC).
  - **#2129 red-team → ZNALEZIONY+NAPRAWIONY realny FINDING HIGH** (PR #2220): bulk-actions gated tylko coarse `bulk_operations`, `delete` nie re-asertował `products.delete` → Marketing kasował produkty (live: 200, produkt 404). Fix: `PER_ACTION_PERMISSION` map + `PermissionCheckerInterface` przed dispatchem → 403. Test `BulkActionsPerActionPermissionApiTest`. Harness `red-team-rbac.sh` (14-pkt §5.3, provisioning Marketing przez invitation dev-token).
  - **#2137** chaos dry-run → degradacja udokumentowana (Meili down→search 503 problem+json, Redis/Mercure→200, MinIO→upload 500 HTML), 2 luki → **#2221** (MinIO upload nie-RFC7807), **#2222** (brak alertów `up==0`). Restart-all czysty.
  - **#2131-2133 deep audit** (3-agentowy fan-out taint + **adwersaryjna weryfikacja empiryczna**, PR #2226) → **ZNALEZIONY+NAPRAWIONY webhook SSRF** (`WebhookDeliveryClient` na plain HttpClient zamiast NoPrivateNetworkHttpClient; tenant admin mógł pivotować na 169.254.169.254/redis/minio). Live-proof: `test_webhook`→redis:6379 statusCode:0 po fixie. **2/5 „CONFIRMED" agentów OBALONE weryfikacją:** Meili addslashes = FALSE POSITIVE (empiryczny bypass→0 hits, #2225), SavedView cross-user = accepted MVP (#2224). BulkActions potwierdzony niezależnie przez 2 agenty (walidacja fixu #2129).
- **✅ #2139 app-side mail** zweryfikowany przez Mailpit (invitation/reset docierają z poprawnym tematem/adresatem/treścią; `.env.dev` routuje `MAILER_DSN=smtp://mailpit:1025`). Deliverability SPF/DKIM = hosting.
- **⛔ Blokery udokumentowane na ticketach:** **#2128** load (k6 niezainstalowany + `pim:load:seed 50k` nadpisuje demo katalog + endurance 2-4h; plan sam uznaje „p95 lokalnie = proxy") — substrat gotowy (#2124), do dedykowanego okna. **#2136** agent live LLM (`tenant_agent_configs`=0, brak klucza Anthropic BYOK) — bloker zewnętrzny (credential), pokrycie testami skryptowanego LLM istnieje.
- **Pozostały Blok C (hosting-gated):** #2138 prod deploy+TLS (wybór hostingu), #2140 RODO/prawne, #2141 UAT IdoSell + soft launch (dostęp partnerów). Wykonalne lokalnie części #2140/#2141 (RODO hard-delete flow, IdoSell import smoke) — kandydaci na kolejną sesję.
- **Nowe tickety follow-up:** #2221 (MinIO upload RFC7807), #2222 (alerty up==0), #2224 (SavedView per-user Faza 1), #2225 (Meili meiliLiteral consistency).
- **Lekcja → memory `bulk-endpoint-permission-escalation`:** bulk/{actionType} gated jedną coarse permission omija per-action authz; sprawdzać per-actionType; provisioning restricted usera przez invitation dev-token.

## 2026-07-04 (późniejsza sesja): GOLIVE TOR 1 — naprawa findingów Bloku A (4/4 blokery dnia 1 CLOSED, nie-blokery w toku)
- **Dyrektywa operatora:** wszystkie testy LOKALNIE (bez hostingu, zaakceptowane odstępstwo); po TOR 1 → TOR 2 bez przerw.
- **✅ 4 blokery dnia 1 — wszystkie CLOSED z fresh-clone live-proofami** (wzorzec: izolowany klon `COMPOSE_PROJECT_NAME` + świeże wolumeny + WSZYSTKIE sekrety niedomyślne):
  - **#2176 JWT keypair** (PR #2201) — entrypoint dev/test autogen `lexik:jwt:generate-keypair --skip-if-exists` + warn-only check-config; flaga `PIM_JWT_AUTOGEN` tylko na api (race z workerem); docs README/ONBOARDING.
  - **#2177 messenger_messages** (PR #2202) — migracja `Version20260704050000` (DDL 1:1 z setup-transports, IF NOT EXISTS, jawny GRANT pim_app) + `auto_setup=0` na wszystkich doctrine DSN. Proof: worker healthy RestartCount=0 od pierwszego bootu.
  - **#2181 S3 creds** (PR #2203) — compose api+worker: `AWS_ASSETS_* ← MINIO_ROOT_*`. Proof: upload parse-preview 200 przy niedomyślnych hasłach MinIO.
  - **#2178 komendy DB** (łańcuch 3 PR: #2204 owner self-elevation + `DROP … WITH (FORCE)` → #2205 advisory lock lifecycle → #2209 worker czeka na locku przed cache:clear) — reset wg docs działa na świeżym klonie bez wiedzy plemiennej; wyścig reset↔ensure-seeded (worker RestartCount=3, wzajemne kill-e) wykryty proofem i ubity lockiem.
- **✅ Nie-blokery zamknięte:** #2186 drift-detektor (PR #2207 — root cause GŁĘBSZY niż lekcja: jsonb kanonicalizuje kolejność kluczy, strict `!==` = wieczny mismatch selectów; fix normalizacja + test select-type) · #2199 401-noise (PR #2206 — fetchInternal czeka na in-flight refresh; spec regresyjny) · #2188 i18n parytet (PR #2208 — 29 realnych braków EN + plural-aware guard CI) · #2196 restore wrapper (weryfikacja fixów z #2197 + drill RTO 19s — closed).
- **⚠️ INCYDENT (proces):** PR #2208 zmergowany mimo 1 fail — uzupełnione klucze EN zmieniły stringi UI dla en-profilu Playwrighta i wywróciły specy polegające na fallbacku → czerwony main. Hotfix #2212: pin języka suite'u (`storageState` z `i18nextLng=pl` w playwright.config). Lekcja w lessons.md (nigdy nie mergować bez sprawdzenia WSZYSTKICH checków).
- **✅ Root-fix korupcji dev-cache (#2179 opcjonalne kryterium, PR #2214):** api(debug=1) i worker(debug=0) kompilowały różne kontenery DI do wspólnego `var/cache/dev`; boot-owy cache:clear workera wycinał debug-kontener spod api („HTTP 200 + HTML fatal"). Fix: `APP_CACHE_DIR` per rola (Kernel::getCacheDir + compose worker `/app/var/cache-worker`). Proof: pełny cykl worker-restart + reset bez żadnego heala.
- **PR-y w CI/kolejce na koniec sesji:** #2210 (literały PL→t(), po #2212 wymaga rebase — sam dodaje klucze EN), #2211 (PHP max-lines guard; #2192 zostaje OTWARTY do refaktoru ImportRunHandler — świadome odejście), #2212 (hotfix E2E), #2213 (docs batch #2179/#2180/#2183/#2182.1), #2214 (cache-dir), #2215 (#2191 CLAUDE.md BC), #2216 (#2193 architektura). #2182: triage pkt 2 = flaky (pass/pass/FAIL/pass), nie deterministyczny — comment na issue; close po merge #2213.
- **Następny krok:** merge kolejki → close #2179/#2180/#2182/#2183/#2189/#2191/#2193 z proofami → **TOR 2: Blok B (#2127–#2137) LOKALNIE** (klucz Anthropic: sprawdzić BYOK Haiku na dev stacku), potem Blok C w zakresie lokalnym (Mailpit zamiast SMTP, symulacja UAT).

## 2026-07-04: Epik GOLIVE — ✅ BLOK A KOMPLETNY (8/8 ticketów #2119–#2126, wszystkie merged + closed z proofem)
- **Plan:** `Project Plan/15-plan-testow-przedprodukcyjnych.md` (3 bloki, 23 tickety #2119–#2141, milestone GOLIVE). Blok A = testy lokalne równoległe-bezpieczne (docs/infra/audyt, bez code freeze). Raporty: **`docs/audit/2026-07-handover/`** (README = raport końcowy + 6 podraportów) + `docs/security/`.
- **Zamknięte z live-proof:** #2119 cold-start (PR #2184) · #2124 load k6+seed (PR #2185) · #2125 fresh-install (PR #2187) · #2120 struktura+dług (PR #2194) · #2123 security docs (PR #2195) · #2122 restore drill (PR #2197) · #2121 licencje+raport (PR #2198) · #2126 i18n+przeglądarki (PR #2190).
- **Werdykt handover: GOTOWY WARUNKOWO** — kod/architektura/testy/backup/licencje zdrowe; blokują TYLKO luki onboardingu dnia 1 (każda z ticketem). Licencje: zero GPL/AGPL/SSPL (PHP 150 prod: 144 MIT + 1 LGPL htmlpurifier; JS permisywne).
- **3 blokery dnia pierwszego (cold-start, klasa „works on my machine" po splicie ról W1-1, niewidoczne w CI):** #2176 brak generacji JWT keypair → login 500 · #2177 `messenger_messages` poza migracjami → worker crashloop · #2181 `AWS_ASSETS_KEY` nie propaguje `MINIO_ROOT_*` → uploady 500. Plus #2178 komendy DB padają na `pim_app`.
- **2 KRYTYCZNE bugi narzędzia DR wykryte+naprawione restore drillem (#2196):** PITR fizycznie NIE działał (`--target` ze spacją łamany przez `${ARR[*]}` join) + brak `--target-action=promote` (klaster zostawał read-only) + re-grant `pim_app`. Bez drillu wyszłoby dopiero w realnym DR. PITR precyzyjny, RTO ≈ 19s.
- **Pozostałe findingi (tickety, nie-blokery):** #2186 detektor dryfu false-positive na `select` (dane OK) · #2188 48 braków kluczy EN i18n · #2189 literały PL poza t() · #2199 401 na /settings/users · #2179/#2180/#2182/#2183 cold-start docs · #2191/#2192/#2193 struktura/guardy.
- **Reindex Meili od zera:** `--purge` obowiązkowy (bez niego sieroty: 646 docs vs 211 obiektów). Harness `scripts/fresh-install-verify.sh`.
- **Load prep:** `scripts/load/` — 8 scenariuszy k6 (products/search/export/bulk + capped smoki login/import/feed) + `pim:load:seed` (kanoniczne object_values, N×M×L) + orkiestrator. Konsumowane przez Blok B #2128.
- **Świadome odejścia:** WebKit console-clean niezweryfikowany (DNS pim.localhost bez /etc/hosts, sudo niedostępne — render OK); pełny PHPUnit lokalnie niewykonalny wg docs (CI-only); load p95 lokalnie = proxy (bezwzględny gate <300ms → Blok B na prod-podobnym HW).
- **Następny krok:** Blok B (po code freeze — koniec DP+FE): baseline CI #2127, load 50k+endurance #2128, red-team #2129/#2130, deep audit #2131-2133, izolacja tenantów #2134, smoke #2135, agent live LLM #2136 (bloker: klucz Anthropic), chaos #2137. Blok C: prod deploy #2138 (bloker: hosting) + SMTP/RODO/UAT.
- **Lekcje → `agent/lessons.md`** sekcje „Lessons z GOLIVE #2119/#2124/#2125/#2126" (izolacja stacka COMPOSE_PROJECT_NAME, klasa bugów po splicie ról, owner-connection dla CLI utrzymaniowych, K6_VUS native-override, rebuild --purge, cross-browser matryca).

## Gdzie jesteśmy
- **Epik:** APIC — Uniwersalny Konfigurator API ([backlog](../Project%20Plan/feature-api-configurator-tickets.md), ADR-0022). Konsument `src/Integration/Generic/`.
- **Backlog ticketów (source of truth speców):** `Project Plan/feature-api-configurator-tickets.md` (Issues #1756–#1803).
- **Stan (2026-06-29):** **CAŁY EPIK APIC KOD-KOMPLETNY i zmergowany do main (M0→M5).** M1+M2+M3 (Sync Engines) + M4 (Monitor + Producent, P4-01..08) + **M5 Hardening (P5-01..05) zmergowane**. Tickety [PM] cross-context (P3-04/05/06/08) wykonane: inbound przez Catalog `InboundRecordWriter` contract + `BatchValueWriter.writeMany(Provenance::Integration)`; outbound przez Export `OutboundRecordReader`; transport `import` (TenantAwareMessage + rebinding/RLS-GUC middleware); ConflictResolver LWW/pim_wins/remote_wins + anti-loop.
- **✅ Dług bookkeepingu issue — ZRECONCYLIOWANY (2026-06-29):** 18 issues APIC (P1-07/08 + całe M2 #1770–1778 + M3-early #1779–1785) były OPEN mimo zmergowanej pracy (slip `Closes #N` z wcześniejszych sesji). Każdy zmapowany 1:1 na swój merged PR (#1816–#1829, #1857–#1860) i zamknięty z proofem merged-PR (decyzja operatora: reconcile z merged-PR zamiast pełnego live-smoke). **Wszystkie 6 milestone'ów APIC: open=0.** Epik APIC = 0 open issues.
- **✅ Epik XMLF — Konfigurator XML (ZAMKNIĘTY 2026-07-02 — 38/38 ticketów, wszystkie milestone'y M0–M6 komplet):** [backlog](../Project%20Plan/feature-konfigurator-xml-tickets.md), ADR-0023, 38 Issues #1902–#1939, milestone'y #34–#40. **Feedy produktowe XML działają end-to-end na żywo: kreator (szablon → struktura custom → scope → mapper → delivery → preview) → publiczny URL z tokenem → monitor z SSE.**
  - **M0–M2 (silnik + model + generator):** XmlWriterCore/ItemWriter/GenericXmlWriter + ExportFormat::Xml + kafelek EXR · FeedProfile/FeedRun/FeedRunLog + RLS + CRUD + persist-guard · FeedDescriptor VO + 3 szablony predef + transformacje + FeedGenerator + XmlFeedWriter + create-from-template + edytor struktury API (P2-07).
  - **M3 (delivery):** mapper API (#2009) · ExportBuilder adapter (#2008) · kategorie external_code + warianty flat item_group_id (#2017) · token service (#2007) · public URL cache-and-serve (#2013) · telemetria pobrań + rate-limit + redakcja tokenu (#2014). **M4 (operacje):** regenerate (#2010) · async + Mercure SSE + cancel (#2015) · monitor API (#2016) · preview (#2012).
  - **M5 (UI, 7 ekranów):** hub (#2018) · wizard szablon+scope (#2019) · mapper (#2020) · delivery+preview (#2021) · detail z live pipeline + drilldown (#2022) · monitor globalny + subnav (#2023) · **edytor struktury custom (#2026)** — kroki kreatora id-based `stepsFor(kind)`, walidacja inline mirror guardu BE, outline na żywo.
  - **M6 (hardening + docs):** injection suite (#2011, 42/42) · **security suite publicznego URL (#2024)** — 2-tenant probe, 404-not-403, entropia 192-bit, rate-limit na 200-kach, cache-only-never-generates, rotate/revoke, token_hash nigdy w API; implementacja M3 bez dziur · **benchmark 50k (#2025)** — peak 171 MiB (<256), delta <9 MiB, płaski RAM, Identity Map stabilna, ~50s na CI runnerze; wariant Ceneo-shape zielony; alert Prometheus 256 MiB potwierdzony · **docs+hooki (P6-04)** — `Project Plan/feature-konfigurator-xml-deferred-hooks.md` (8 [DEF] hooków z reuse-mapą) + `docs/export/feeds.md` (user-docs, flow zweryfikowany curl+xmllint).
  - **Świadome odejścia (udokumentowane w PR-ach):** `/api/feeds/stats` z speców UI nigdy nie istniało → KPI = merge `/api/feed-runs/kpi` + `/api/feeds/pull-stats`; `next_run` z crona = null do czasu serwisu harmonogramu (FeedScheduleService renderuje human-readable); first-publish po kreatorze = manual regenerate trigger (dedykowany enqueue to P4-01 scheduler follow-up); publication-profile select nieobecny (BE nie eksponuje ChannelPublicationProfile — ADR-0018 follow-up); CSV/download logów drilldownu = hooki; cel czasowy 30s benchmarku miękki (log + twardy sufit 120s na CI).
  - **Gotcha (skrót; pełne wzorce w lessons.md sekcje XMLF):** zmiana interfejsu łamie anonimowe fake'i → uzupełniać. Raw SQL wymaga `// tenant-safe:` (lint skanuje tylko src/). `em->clear()` detachuje → rebind/reload. Guard ADR-0021 liczy string co-occurrence nawet w komentarzach → stream hooks w osobnych plikach. Playwright: glob `*` nie łapie `/`, dwujęzyczne lokatory, `/1\s?284/`, axe kontrast zinc-500, regresję speców uruchamiać na branchu który FAKTYCZNIE jedzie do CI. ApiTestCase: bezpośredni `dispatch()` nie rebinduje RLS GUC (set_config ręcznie jak scopeToTenant), a współdzielony kernel trzyma TenantFilter z ostatniego requesta → `TenantContext.clear()+apply()` przed anon pullami.

## 2026-07-03: VIEW-13 — Dashboard „Centrum dowodzenia katalogiem" (pixel-perfect mock relayout)
- **Ticket:** `Project Plan/UI/Wdrozenie_grafiki/ticket-VIEW-13-dashboard-command-center.md`, Issue #2143. **Scope: FRONTEND-ONLY, dane statyczne** — backend per widget dorabiany później (`dashboard-do-oprogramowania.md`).
- **Zrobione:** nowy layout `features/dashboard/page.tsx` (greeting → ciemny agent hero → 4 KPI → [Zdrowie danych | Aktywność] → Centrum akcji) + 6 nowych komponentów + `mocks.ts` (kształt 1:1 pod przyszłe DTO). Usunięte: 11 starych widgetów v2 + `mock-data.ts` + amber `DashboardMockBanner` + pill `AuditLogStatus` z topbara (decyzje operatora: zero labelek MOCK na dashboardzie). Hooki `use-dashboard-counts`/`use-dashboard-completeness` zostają do re-wiringu.
- **Gates:** typecheck/lint/vitest 114/114/build zielone; Playwright view-13 (render + axe WCAG A/AA) 2/2 zielone; wizualna weryfikacja screenshotem vs mock OK.
- **Gotcha:** Playwright lokalnie — `apiLogin` (page.request, Node) nie resolvuje `pim.localhost` bez wpisu w /etc/hosts (Chromium resolvuje sam) → w specu form-based `loginAsAdmin`; język w testach deterministycznie przez `localStorage.i18nextLng='pl'` w addInitScript (detektor bierze navigator=en-US z domyślnego profilu).
- **Następny krok:** operator manual smoke; potem tickety podpięcia BE per widget.

## 2026-07-03: Epik 0.7 AGENT — ✅ CAŁY EPIK ZMERGOWANY DO MAIN (49/49 ticketów, M0–M9; merge train zakończony)
- **Backlog:** `Project Plan/feature-agent-tickets.md` (49 ticketów #1944–#1992, M0–M9), PRD `PRD/PRD-PIM-agent.md`, **ADR-0024 accepted** (usuwalny BC + rejestr narzędzi + single approval gate). **Raport końcowy:** `docs/adr/agent-epic-0.7-final-report.md`.
- **Stan (2026-07-03, po merge trainie):** wszystkie PR-y #2085–#2118 na main. Tickety AGENT-* zamknięte; stare tickety 0.7.x (#63–#71) zamknięte z mapowaniem na dostarczone PR-y; częściowo pokryte UI-owe (#108–#112) skomentowane ze statusem i zostają otwarte (token-streaming, accept-selected, bulk-approve, badge→run link). **Go-live gate: #2136** (live smoke z realnym kluczem Anthropic — BYOK).
- **Merge train (post-mortem skrót):** 28 PR-ów zmergowanych sekwencyjnie; per PR „formality merge" `origin/main -X ours` + asercja niezmienionego drzewa (wtedy zielone CI z head-SHA pokrywa merge-commit bez ponownego czekania). Czerwone checki w ~90% to ARTEFAKTY `cancel-in-progress` (każdy push/merge rusza merge-refy pozostałych PR-ów i kasuje ich Playwrighty w locie — logi pokazują „141 passed" + „operation was canceled"). Realne faile: 3 (fixture TriggerExportTest bez built-in OT → backport fixu z P4-01; spec-drift #2093 z konstrukcji → cherry-pick snapshotu z #2094; literal namespace'u modułu w docblockach → przeredagowane, także we własnym AgentRemovabilityTest P9-05). GitHub gotcha: kasowanie brancha BAZOWEGO zamyka (nie retargetuje) stacked PR-y i zamkniętego nie da się reopenąć bez przywrócenia bazy → obejście: restore bazy → reopen → retarget na main → delete; prewencyjnie cały stack zretargetowany na main przed trainem.
- **Smoke na żywo (main, pim.localhost):** login → `/products` renderuje (bez błędu seedera) → Cmd+K otwiera paletę → `/settings/ai` pokazuje BYOK status (Disabled soft-off, prefix) + sekcję kosztów/limitów. Wcześniejszy błąd „Built-in product ObjectType not found" = FrankenPHP dev-cache corruption (HTML z HTTP 200 na części workerów), NIE brak seeda — heal: nuke `var/cache/dev` + warmup + restart api/worker.
- **M3 write (UC2):** P3-01 bulk_edit_values #2085 (merged) · P3-02 approve→commit #2086 (merged) · P3-03 reject/cancel #2087 · P3-04 rollback kanoniczny #2088 · P3-05 assign_categories #2089 · P3-06 trigger_export #2091 · P3-07 audyt DH #2092.
- **M4 API:** P4-01 8 endpointów `/api/agent/*` + resume transkryptu + canned LLM #2093 · P4-02 OpenAPI snapshot #2094.
- **M5 schema-ops (Opus):** P5-01 create_attributes_from_schema #2096 · P5-02 modeling tools #2097 · P5-03 per-kind model #2098 · P5-04 schema rollback dataless-only #2099.
- **M6 frontend:** P6-01 chat Sheet #2100 · P6-02 Cmd+K real #2101 · P6-03 inbox diff #2102 · P6-04 historia+rollback #2103 · P6-05 provenance badge #2104 · P6-06 BYOK settings #2105 · P6-07 SSE #2106.
- **M7 engine-gated:** P7-01 feed tools (XMLF) #2107 · P7-02 publish (Integration seam) #2108.
- **M8 proaktywność:** P8-01 data steward #2111 · P8-02 AI-mapping #2110 · P8-03 multi-step intents #2112 · P8-04 webhooks #2109 · P8-05 ObjectTypes+autonomia #2113.
- **M9 hardening:** P9-01 red-team + EM-detach hardening #2115 · P9-02 cost dashboard #2116 · P9-03/04 threat-model+runbook+user-guide #2117 · P9-05 removability test + raport #2118.
- **⚠️ Regresja main naprawiona (#2086):** zgubiony `imports: services_agent.yaml` — agent ładował się przez `App\` discovery bez tagów `agent.tool` → **pusty rejestr narzędzi**, niewidoczne dla CI (żaden test nie bootuje rejestru z kontenera). Objaw: `test.agent.approval` service-not-found. Fix: przywrócenie importu; `AgentRemovabilityTest` (suite architecture) łapie leak `App\Agent` lokalnie.
- **⚠️ Bug produkcyjny wykryty red-teamem (#2115):** write-tool czyszczący EntityManager (chunk flush+clear) odczepiał run/tenant/wiersze audytu → skompromitowany tool mógłby **stłumić własny wpis audytowy** wymuszając clear, a plany wielo-write crashowały na flush. Fix: `GuardedToolExecutor` buduje `AgentToolCall` na przeładowanym zarządzanym runie (tenant przypisany wprost); pętla reattachuje run+tenant przed persistem.
- **⚠️ BYOK:** brak realnego klucza Anthropic — LLM-live smoke niewykonalny. Cały cykl UC2 (∅→100→approve→commit→rollback) i UC1 (schema IdoSell) pokryty testami integracyjnymi ze skryptowanym `AgentLlmClientInterface`; red-team dowodzi odporności na injection przez approval+RBAC. Pełen live smoke = operator po wprowadzeniu klucza (runbook `docs/operations/agent-runbook.md`).

### Epik 0.7 AGENT — historyczny snapshot startu (M0 KOMPLETNY, M1 prawie, M2 w PR-ach)
- **M0 KOMPLETNY (9/9, wszystkie merged+closed z proofem):** P0-01 ADR (#2053) · P0-02 Deptrac Agent_Internals/Contracts + Import/Search_Contracts (#2059) · P0-03 pending_changes+port (#2064) · P0-04 Provenance::Agent (#2055) · P0-05 EntityChanged (#2057) · P0-06 Anthropic SDK+BYOK factory (#2058) · P0-07 tool registry [SEC] (#2061) · P0-08 AgentFeatureGuard (#2062) · P0-09 removability CI job (#2066, **job `agent-removability` = bramka DoD epiku**; szew DI: `services_agent.yaml` + `packages/agent.yaml`, główny discovery excluduje `src/Agent/`).
- **M1:** P1-01 encje agent_runs/messages/tool_calls + RLS + migracje w namespace modułu `AgentMigrations` (#2063 merged; fix `TimestampVersionComparator` — patrz lessons) · P1-02 RBAC per tool-call [SEC] (#2065 merged) · P1-05 Mercure agent-runs topics (#2067 merged) · **P1-03 pętla async (PR #2079, CI)** · **P1-04 limity §8.5 [SEC] (PR #2080, stack na #2079)**.
- **M2 (read/grounding):** P2-01 search_catalog + `Search\Contracts\CatalogQueryPort` (PR #2069, CI) · P2-02 aggregate_count (branch, PR po #2069) · P2-03 completeness_report + `Catalog\Contracts\Query\CompletenessReportPort` (branch, PR po P2-02).
- **⚠️ BYOK:** brak realnego klucza Anthropic — LLM-live smoke niewykonalny; wszystko budowane testowalnie z mockowanym transportem (scripted `AgentLlmClientInterface`), PR-y LLM-zależne jawnie „LLM-live smoke pending BYOK key". Ścieżka bez klucza (guard-deny 403) smoke'owana na żywo.
- **⚠️ Working tree współdzielony:** w trakcie maratonu pojawiły się cudze unstaged bumpy manifestów (composer/package jak z dependabota) → odłożone na `git stash` (label `foreign-worktree-changes`); vendor w kontenerze zdryfował do phpstan 2.2.3 przy locku 2.2.2 → naprawione `composer install`.
- **Następny krok:** merge #2069→P2-02→P2-03 i #2079→#2080; potem M3 (P3-01 bulk_edit_values → P3-02 approve/commit → P3-03 reject/cancel → P3-04 rollback → P3-05 kategorie → P3-06 trigger_export → P3-07 audyt) = UC2, dalej M4 API → M6 FE.

## 2026-07-03: ✅ Epik DP — Drobne poprawki (MARATON ZAKOŃCZONY — 8/9 zmergowane, 1 świadomie zaparkowany)
- **Backlog:** label `epik-DP`, 9 issues **#2031–#2039**; research 5 agentów Explore (2026-07-02); wykonanie w jednym maratonie 2026-07-03.
- **Merged (8):** DP-03 #2166 (kanały+locale→Modelowanie, redirecty) · DP-04 #2167 (`?attributeGroup=` junction-OR-legacy + combobox URL-state) · DP-01 #2168 (MoveCategoryDialog nad gotowym BE VIEW-04; 409 impact-gate w UI) · DP-05 #2169 (ValidationRulesSection per-typ + live-tester regexa; **odkryty dryf docs↔engine: price max_amount deklarowany, nieegzekwowany**) · DP-02 #2170+#2171 (admin set-password `POST /api/users/{id}/password` + `revokeAllForUser` + modal; email=login bez skrzynki) · DP-06 #2172 (iso_country/url/require_https + price max_amount — dryf domknięty) · **DP-07 #2173 (ADR-0025**: `object_types.validation_rules` JSONB, CompareRule+RequireWhenRule reużywa VisibleWhenRule, `CrossFieldRulesValidator` w OBU write-pathach, upserter 3-fazowy z atomowym 422-przed-zapisem, builder na detail OT; plan-mode approved) · **DP-08 #2174** (conditional visibility czysto FE na ISTNIEJĄCYM substracie visible_when z UI-08.8 — ewaluator TS 1:1, gating w formularzu produktu, edytor reguły; wartości ukrytych pól nie giną).
- **DP-09 #2038:** świadomie NIE implementowany (default per ticket: optional) — komentarz decyzyjny na issue, zostaje otwarty jako zaparkowany backlog (trigger: pierwszy realny use case cross-unit).
- **Live smoki (pim.localhost, wszystkie flow):** DP-04 filtr 125→37 · DP-05 max_length→422 · DP-02 pełny cykl konta bez skrzynki · DP-06 „Polska"→422/„PL"→200, http→422/https→200 · DP-07 compare 50v10→422 z NIETKNIĘTYMI wartościami (atomowość) · DP-08 hide/reveal z przeżywającą wartością.
- **Lekcje → `agent/lessons.md` sekcja „Lessons z epiku DP"** (timeout-minutes 20 maskujący się jako flake — podniesiony do 30 w #2171; gitleaks per-commit; guard ADR-0021 vs race merge-refów + wzorzec mount-on-open; locator-pitfall div.filter().first(); serializer cache:clear; cs-fixer vs inline @var; attributes_indexed nieużywalny na write-path).

## 2026-07-03: Epik GOLIVE — plan testów przedprodukcyjnych (backlog utworzony, realizacja niezaczęta)
- **Co:** plan wszystkich finalnych testów przed go-live. Dokument `Project Plan/15-plan-testow-przedprodukcyjnych.md`. Label `epik-GOLIVE`, milestone #51, **23 issues #2119–#2141** w 3 blokach sekwencyjnych.
- **Blok A (równolegle z epikami DP/FE, ~40–55h):** handover-readiness audit (cold-start #2119 + struktura/dług #2120 + licencje/raport #2121), restore drill pgBackRest+PITR #2122, threat-model+security-checklist #2123, przygotowanie load k6+seed50k #2124, fresh-install migracje/reindex #2125, i18n+przeglądarki #2126.
- **Blok B (po code freeze, ~50–70h):** baseline CI #2127, load 50k+endurance #2128, red-team 15-pkt #2129, regresja 5 CRITICAL #2130, **deep audit multi-agent** (setup+taint #2131 → sweep+weryfikacja #2132 → PoC regresyjny #2133), izolacja tenantów na żywo #2134, smoke krytycznych ścieżek #2135, agent live LLM #2136, chaos+alerting #2137.
- **Blok C (produkcja+launch, ~25–35h):** deploy prod+TLS+sekrety #2138, SMTP+monitoring+rollback #2139, RODO+prawne #2140, UAT IdoSell+soft launch #2141.
- **Lokalnie vs hosting:** Blok A i B w pełni lokalnie na `pim.localhost` (B6 wymaga klucza Anthropic, nie hostingu; B1 = CI). Blok C wymaga hostingu — nie da się lokalnie tylko: deliverability SMTP (SPF/DKIM/inbox) i soft launch (dostęp partnerów). Load p95 lokalnie = proxy, bezwzględny gate <300ms potwierdzić na prod-podobnym HW.
- **B3+ deep audit (dodane na życzenie operatora):** semantyczne śledzenie taint (źródło→ujście) ponad skanery; wiel-agentowy fan-out + adwersaryjna weryfikacja; PoC regresyjny (proof-of-fix) dla każdego HIGH/CRITICAL. Tickety security mają defensywny framing autoryzacyjny (redukcja dual-use model-switch). Kompensuje brak zewnętrznego pentestu (odłożony do fazy SaaS — residual risk zaakceptowany).
- **Blokery:** klucz API Anthropic (B6 #2136), wybór hostingu (C1a #2138).
- **Następny krok:** Blok A startuje od razu. Blok B po zamrożeniu kodu (koniec DP+FE). PR planu: #2150 (czysty rebuild po tym jak #2142 odbił się od nieaktualnego detached HEAD i pokazywał 6 niezwiązanych plików agenta — zamknięty).

## APIC — postęp implementacji (2026-06-28/29)
- **M1 Consumer Foundation:** P1-01..09 + P1-07 hub (#1816), P1-08 wizard 1–2 (#1817).
- **M2 Descriptor + Mapping (KOMPLETNE):** P2-01..09 (#1818–#1826). P2-10/11 = deferred §7 hooks.
- **M3 Sync Engines (KOMPLETNE):** P3-04 InboundRecordWriter+runner (#1857), P3-05 inbound routing+RLS-GUC, P3-06 OutboundRecordReader+runner (#1859), P3-07 outbound trigger (#1860), P3-08 ConflictResolver (#1861), P3-09 scheduler cron+jitter (#1862), P3-10 SyncBinding CRUD+run/pause (#1863), P3-11 sync-config FE (#1864), P3-12 connection detail 5 tabs (#1866).
- **M4 Monitor + Producent (KOMPLETNE):** P4-01 SyncRun history API (#1865), P4-02 monitor FE (#1867), P4-03 per-profile OpenAPI export (#1870), P4-04 builder-options endpoint (#1869), P4-05 webhook retry + delivery-history (#1868), P4-06 producer hub FE + webhook read API (#1871), P4-07 profile builder FE (#1872), P4-08 unify shell FE (#1873).
- **M5 Hardening (KOMPLETNE, 2026-06-29):** P5-01 cross-tenant RLS isolation suite (#1874, fix identity-map: `find()`-by-PK omija TenantFilter → `em->clear()` przed cross-tenant readami), P5-02 SSRF+descriptor adversarial slice (#1875), P5-03 connection-credential rotation + needsRotation + runbook (#1876), P5-04 sync benchmark + **fix O(rekord)→O(batch) w obu runnerach** (#1877; `flush()`-bez-`clear()` w pętli pull/push → per-page/per-200 `clear()` + reload managed SyncRun/SyncBinding + re-point TenantContext; `SyncMemoryBenchmarkTest` grupa import-benchmark), P5-05 consumer/producer guide (#1878). Pozostałość: Export reader full-set + N+1 = cross-context follow-up (`docs/perf/sync-engine-benchmark.md`).
- **⚠️ Numery issue NIE są pochodną sekwencyjną** — weryfikuj `gh issue list --search "APIC-PX-NN in:title"` przed `Closes #N` (np. P3-09=#1787, P4-06=#1796, P4-08=#1798).
- **Wzorzec per ticket:** branch → bramki w kontenerze api (PHPStan max + Deptrac + CS-Fixer + PHPUnit; Api/Integration = CI-only) → FE bramki na hoście (tsc + biome + vitest + vite build + max-lines guard ≤500 linii; Playwright lokalnie na `pim.localhost` po `restart redis`) → OpenAPI regen `api:openapi:export | python3 -m json.tool` → PR → justified-merge. **Gotchas: custom `#[Route]` pod `/api/api_profiles/...` shadowowany przez AP `{id}` → użyj `/api/profiles/...`. OpenAPI z kontrolera = `NormalizerInterface->normalize($openApi,'json')`, nie json_encode. jsdom axe nie liczy kontrastu (text-zinc-400 pada AA → zinc-500); Playwright getByText łapie subtitle → role-locator.**

## (poprzedni epik) IMP2 — Import/Export v2 engine
- **Epik:** IMP2 ([#1499](https://github.com/malipie/PIM/issues/1499), ADR-0019). Kontrakty silnika + warstwy Deptrac Import/Export.
- **Backlog ticketów (source of truth speców):** `Project Plan/UI/feature-imports-v2-tickets.md` (Issues #1460–#1499).

## 2026-07-01: Epik XMLF — Konfigurator XML (backlog utworzony, implementacja niezaczęta)
- **Co:** rozpisanie planu `feature-konfigurator-xml-plan.md` + zaprojektowanych widoków (`Feedy.html` + `feedy/*.jsx`) na wykonalny backlog (brief §10). Cel: feedy produktowe XML (Google Shopping / Ceneo / Meta / custom B2B) z mapowaniem, filtrem, harmonogramem i stabilnym URL + XML jako format eksportu ad-hoc — jako warstwa „szablon + mapowanie + transformacje + delivery" NAD silnikiem Export (reuse `ExportBuilder`, nowy `ItemWriter` asocjacyjny, `Export/Feed` pod-obszar).
- **Deliverables:** **ADR-0023** (`docs/adr/0023-konfigurator-xml-placement.md`, draft do finalizacji w P0-01). Backlog **`Project Plan/feature-konfigurator-xml-tickets.md`** = 38 ticketów, prefix `XMLF`, 7 milestone'ów M0–M6. Designy: `Zrodla/.../PIM-nowoczesny/feedy/*.jsx` (hub, kreator 5-krokowy, mapper, preview, monitor).
- **GitHub:** label `epik-XMLF` + milestone'y M0–M6 (#34–#40) + 38 issues #1902–#1939 (cross-linki blocked_by/blocks jako `#refs`). Hooki §7 (import XML, push FTP, Allegro, AI-mapping, adaptive scheduling) bez issue do czasu wejścia w scope.
- **Proces przygotowawczy:** stan as-is zweryfikowany 3 agentami Explore (silnik Export, Channel/Schedule/Filter, security/infra); backlog przepuszczony przez 2 adwersarialnych krytyków (kompletność + graf zależności) → 4 luki domknięte (edytor struktury custom P2-07/P5-06, telemetria pobrań P3-06, globalny monitor P5-07), własność kształtu descriptora rozstrzygnięta (P2-01 VO kanoniczny), twarde prereq serializacji P0-03/P0-04→P2-04/P2-05 dodane.
- **Następny krok:** XMLF-P0-01 (finalizacja ADR-0023, [PM]) → P0-02 (Deptrac `Export_Feed`) → P0-03 (XmlWriterCore, [SEC]) → P0-04/05/06 (ItemWriter + ExportFormat::Xml + odblokowany kafelek EXR = tryb 2 dostarcza wartość najwcześniej).
- **Blokery:** brak. Tickety [PM]: P0-01, P1-01, P1-04, P2-04, P2-07, P3-04, P3-05, P4-04, P6-02 — wymagają Plan Mode przed implementacją.

## 2026-06-26: Epik APIC — Uniwersalny Konfigurator API (backlog utworzony, implementacja niezaczęta)
- **Co:** rozpisanie planu `feature-api-configurator-uniwersalny-plan.md` na wykonalny backlog (brief §10). Cel: warstwa mapująca PIM ↔ dowolne REST/JSON API (oba kierunki, harmonogram), konsument (`src/Integration/Generic/`, greenfield) + producent (`src/ApiConfigurator/`, domknięcie zalążka).
- **Deliverables:** **ADR-0022** (`docs/adr/0022-...md` + streszczenie §13; numer skorygowany z błędnego „ADR-016" — ADR-0016 to format kluczy API). Backlog **`Project Plan/feature-api-configurator-tickets.md`** = 48 ticketów MVP + 8 hooków, prefix `APIC`, 6 milestone'ów M0–M5. Designy: `Zrodla/.../integracje/api-*.jsx` (8 ekranów).
- **GitHub:** label `epik-API-CONFIG` + milestone'y M0–M5 + 48 issues MVP (utworzone w sesji backlogowej; hooki §7 bez issue do czasu wejścia w scope).
- **Następny krok:** APIC-P0-01 (ADR review) → APIC-P0-02 (Deptrac `Integration/Generic`+Contracts) → APIC-P1-01 (Connection entity). Ścieżka krytyczna do pierwszego inbound sync: 15 ticketów BE/SEC z M0–M3.
- **Blokery:** brak. Tickety [PM] (cross-context/architektura): P0-01/02, P1-01/03, P3-04/05/06/08 — wymagają Plan Mode przed implementacją.

## Najnowsze bug-fixy (2026-06-20, post-Wave 3)
- **#1673** `fix(catalog)` — walidacja zapisu egzekwuje atrybuty wymagane-w-grupie (PR #1674, merged `d0260c34`). Gwiazdka „wymagane" pokazywała `is_required||is_required_in_group`, save-guard sprawdzał tylko `is_required` → pole z gwiazdką zapisywało się puste. Wspólny helper `isAttributeRequired`; blokada **w edit** (create global-only, by nie blokować szkicu). Live proof: spec `1673` + unit 6/6 + produkt `019ee1a6` `description.is_required_in_group=true`.
- **#1678** `fix(import)` — kafelkowy krok „Wybierz dane do importu" w kreatorze (PR #1679, merged `37b8f902`). Analogiczny do eksportu, reużywa generyczne `SelectableCard`/`SelectableCardGroup`: Produkty / Moduły własne (dropdown custom OT) / Kategorie aktywne; Schemat modułów + Atrybuty i grupy „wkrótce". Kreator 6→7 kroków; wybór mapuje na `targetObjectTypeId`. **Rework błędnego #1675/#1676** (operator chciał kafelków jak eksport, nie Combobox „Co importujesz?"). Live proof: spec `1675`+`1429`+a11y zielone, import do custom typu success 2/2. Schemat/Atrybuty = osobne tickety (import per ObjectType, nie metadane).

## Postęp epiku IMP2
- **Fala A — KOMPLETNA (8/8):** #1460–62 quick-winy, #1463 ADR-0019, #1464 kanon JSONB, #1465 tryby CREATE/UPDATE/UPSERT, #1466 ValueWriteCore+BatchValueWriter, #1467 golden round-trip v0, #1468/#1509 transport Messenger `import`+worker.
- **Fala B — w toku:**
  - [x] **IMP2-1.6 (#1469)** — gramatyka kolumn `code.locale.channel` + zapis channelId. **MERGED** (PR #1510, `440a63a3`, 2026-06-13). Live smoke 3/3 na pim.localhost, issue zamknięte z dowodem. Grammar przez `Channel\Contracts\ScopeEnumerator` (deptrac-clean, channelId raz/sesja), export 3-segment + fan-out #1146 + R-47 preflight 422, kolizja kanał↔locale (oba kierunki), golden + kanały.
  - [x] **IMP2-1.7 (#1470)** — multi-kategorie pipe-split + import status/enabled. **MERGED** (PR #1511, `5dd98b8c`, 2026-06-13). Live smoke 3/3, zamknięte z dowodem. CREATE multi-kat (primary+position), replace/append (D2), status/enabled z kolumn (`__status__`/`__enabled__`), FE StepMapping + Playwright, golden round-trip.
  - [x] **IMP2-1.8 (#1471)** — warianty parent_sku two-pass + relacje ObjectRelation + fan-out + galerie + variant_axes. **MERGED** (PR #1512, `0722c50e`, 2026-06-14). Live smoke ✅ (generate-variants→export include_variants→reimport renamed→psql parent_id+variant_axes), #598 zamknięte z dowodem. Dwuprzebieg (`RelationImportStep`): pass-1 buforuje kody, pass-2 resolve tenant-scoped. Galerie pipe-split asset_id + prefetch istnienia per chunk. variant_axes pełny shape `code:v,v|code:v`. **Item 2 (pole fazy checkpoint) DEFERRED** — substrat checkpoint z 1.6a nie istnieje (#1509 = transport only), pełny resume to IMP2-2.3; dwuprzebieg już redelivery-safe (idempotentny upsert + dedupe trójki).
  - [x] **IMP2-1.9 (#1472)** — izolacja błędów per wiersz + severity + partial. **MERGED** (PR #1513, `192afe78`, 2026-06-14). Live smoke `dirty.csv` ✅ (partial 5/2, skip 1, raport z numerami wierszy), #1472 zamknięte z dowodem. severity-driven `isRowBlocking` (Warning nie blokuje), dup SKU→skip (D1), pre-check setowy identifierów (JSONB `value->>'value'`), wiersz śmieciowy→błąd wiersza, recovery przez ManagerRegistry po zamknięciu EM. **Item 3 per-row replay DEFERRED→IMP2-2.3** (EM-lifecycle, ServiceEntityRepository cache'uje EM).
  - [x] **IMP2-1.10 (#1473)** — golden CSV+XLSX matryca + pierwsze testy async. **MERGED** (#1514).
  - [x] **IMP2-1.11 (#1474)** — higiena backlogu (8→1 open IMP-16..19). **MERGED** (#1515).
  - [x] **IMP2-1.12 (#1475)** — media z URL. **MERGED** (#1516, `c0aa5c01`). Live smoke ✅ (async: import→consume→success, envelope `{asset_id}` nie URL, dedup content_hash, link, gating, TenantStamp), #1475/#600/#604 zamknięte. Szew `Asset\Contracts\AssetIngestor` (decyzja operatora). **Adversarial review (16 agentów) złapał 3 critical** → NoPrivateNetworkHttpClient (SSRF rebinding+redirect), TenantStamp, atomowy decrement — naprawione przed merge.
  - [x] **IMP2-1.13 (#1476)** — media z ZIP. **MERGED** (#1517, `d31d42b5`). Live smoke ZIP ✅ (extract case-insensitive+subdir, link, success, zip-deleted), #1476 zamknięte. `ZipImageExtractor` (NFC/NFD, traversal, zip-bomb), controller zip_file≤500MB→MinIO+image_source, reużycie pipeline 1.12. 2 fixy CI (libzip OVERWRITE, tempnam string|false).

### ✅ ETAP 1 IMP2 KOMPLETNY (1.1–1.13) — silnik importu v2 z mediami
### ETAP 2 — w toku
  - [x] **IMP2-2.1 (#1477)** — streaming readers (openspout XLSX, league/csv stream CSV). **MERGED** (PR #1518, `1ee88ae8`, 2026-06-14). Live HTTP smoke na pim.localhost ✅ (bosch.csv dup+puste nagłówki→200/51 wierszy/zero SyntaxError; GA_List.csv 10MB/90k→200/0.88s/90032), #1477 zamknięte z dowodem. ImportRowReader+FileParserService strumieniowo (`Reader::from`+iconv stream-filter / openspout row iterator), detekcja na 16 KiB prefix, `HeaderNormalizer` dedup + mapowanie POZYCYJNE (duplikaty/puste kolumny bez `getRecords($header)` throw). Adversarial review (29 agentów, 8/25 confirmed): UTF-8 boundary misdetect + suffix collision + empty-prefix guard naprawione; CP1250/ISO + billion-row DoS świadomie deferred (→2.7). CI fix: memory test = delta nad baseline (absolutny peak failował przez arena 121MB pełnego suite).
  - [x] **IMP2-2.2 (#1478)** — staged upload. **MERGED** (PR #1519, `5948dd1c`, 2026-06-14). Live HTTP smoke ✅ (upload 1× → validate 200/51 → start 202 server-side copy → unknown id 404 → purge dry-run), #1478 zamknięte z dowodem. `import_staged_files` (TenantScoped + RLS `app.current_tenant`), `StagedFileService` (stage/resolveOwned/downloadToTemp/copyToKey), parse-preview→`staged_file_id`, validate+start przyjmują id (owner-scoped 404, back-compat multipart), `pim:import:purge-staged` TTL 24h (batch flush/tenant), FE stagedFileId+StepDetect capture+StepValidation debounce 500ms. Adversarial review 28 agentów (7/24): 2 stream-leak + purge batch + basename defence naprawione; 404-echo/FK-RESTRICT/FORCE-RLS accept.
  - [x] **IMP2-2.3 (#1479)** — pauza/wznowienie/anulowanie + checkpoint crash-resilient. **MERGED** (PR #1520, `ab047c8a`, 2026-06-14), #1479 zamknięte z dowodem (PHPUnit 6/6 real kernel + live async 2000 success + crash-recovery 3000 bez dup + cancel 200). Checkpoint ATOMOWY z flushem chunka, per-chunk halt (status z `refreshSession`) + `lock->refresh()`, resume pomija ZAPISY ≤ checkpoint ale re-buforuje linki, finalizacja RAW-SQL status-check (nie nadpisuje pauzy→completed). `checkpoint_offset/phase/paused_at`+migracja, `markRunning` idempotentny + handler early-return na Paused, resume RE-DISPATCHUJE ImportRunMessage, FE przyciski+Playwright. Review 28 agentów→6 distinct fixes (wszystkie naprawione). Tests `ImportPauseResumeTest` 6/6 + regresja 222 OK. Live: async 2000→2000 obiektów success; crash-recovery 3000 (worker padł→restart→3000, 0 dup, checkpoint=null); cancel endpoint 200. Pauza-interception=PHPUnit (warm worker <2s, za szybki na HTTP-poll).
  - [x] **IMP2-2.4 (#1480)** — undo-log + rollback v2. **PR otwarty** (`feat/imp2-2.4-undo-log-rollback-v2`). Live smoke ✅ na pim.localhost (import #2 nadpisuje+tworzy → rollback-preview 200 `{created_to_delete:1,values_to_restore:2}` → POST rollback 200 `{deleted_objects:1,restored_values:2,skipped_superseded:0}` → psql: name cofnięte do SmokeOld + provenance=import + created object skasowany). Tabela `import_undo_log` (TenantScoped + RLS), `ImportUndoLogger` (capture before-state w gałęzi UPDATE, reuse `ValueWriteCore::routeScope`, first-write-wins), rollback v2 (replay restore/remove + manual-edit guard + **superseded guard** dla stackowanych importów + Meili ghost-doc fix + attributes_indexed rebuild), `GET rollback-preview`, purge w `pim:import:purge-staged`. Adversarial review (13 agentów, 4 confirmed) — WSZYSTKIE naprawione przed PR: provenance_meta restore, supersededScopeKeys (UUIDv7 chronologia), markScopesCaptured (resume first-write-wins), window-race authorized-at. **Deferred** (świadomie, w PR body): undo dla object-field/category/relation (tylko value-undo = headline AC), konfigurowalny próg size (kolumna `undo_log_enabled` jest, logika TBD bo kłóci się ze streamingiem), FE preview modal.
  - [x] **IMP2-2.5 (#1481)** — tenant_id na import_logs + RLS + GUC `app.current_tenant` w workerach. **PR #1522 otwarty** (`feat/imp2-2.5-import-logs-tenant-rls`). Live smoke ✅ (migracja: tenant_id NOT NULL + RLS 2 polityki + **backfill 0/9565 NULL**; async import → log z tenant_id == tenant sesji). `ImportLog implements TenantScoped` (auto-stamp, 4 call-site'y nietknięte), `TenantRlsGucMiddleware` (session-level GUC + reset w finally, po TenantContextRebinding), `TenantAuditCommand` weryfikuje teraz kolumnę. Testy: `ImportWorkerRlsGucTest` + `importLogsAreIsolatedByTenantFilter`, regresja 99/99. Review (23 agenty, 1 „confirmed" → odrzucony: FOR ALL policy USING=WITH CHECK pokrywa INSERT, dowód live + import_undo_log).
  - [x] **IMP2-2.6 (#1482)** — perf bulk-path + benchmark. **KOMPLETNY, 3 sub-PR-y MERGED, #1482 zamknięte z live proof** (decyzja operatora — smoke każdego kawałka):
    - [x] **cz.1 EKSPORT** (PR #1545): `resolveTargetCount` COUNT + `runToFile` streaming scope-All (keyset + clear/strona) + benchmark 5k/50k <256MiB. Live smoke 5100 prod, worker RAM ~40 MiB.
    - [x] **cz.2 IMPORT** (PR #1546): BulkContext ON + async rebuild + batch Meili reindex + drain middleware + compare-values diff + benchmark. **Diagnoza pamięci (operator: „zbadaj event-cascade memory"): BRAK realnego wycieku** — worker importu płaski **~95 MiB @50k** (prod/`APP_DEBUG=0`). Pierwotne 815 MiB @50k = 3 artefakty test/dev: (a) profiler Doctrine `DebugDataHolder` (debug=1 lokalny default test) zbiera każdy SQL ~15MiB/1k, (b) `InMemoryMercureHub` retainuje każdy Update, (c) inline rebuild przy `async=sync`. Benchmark mierzy teraz `ImportRunHandler::rowPhasePeakBytes()` (worker w izolacji) + `stopRetaining()`.
    - [x] **cz.3 THROTTLE+FE** (PR #1547): usunięty per-row `row_processed` (50k→~100 eventów), `progress()` throttlowany co ~1%, `error` per blokujący wiersz, FE `useImportProgress` live-log z `error`+`progress`. Twardy test `batchImportThrottlesProgressInsteadOfPerRowEvents`.
    - Smoke #1482: login 200 + import 600/600 success + `attributes_indexed` rebuilt + Meili `objects` 603 SMOKE hits (status `draft` — editorial, nie defekt). Smoke utworzył 600 `SMOKE-*` draft w demo (do cleanup).
  - [x] **IMP2-2.7 (#1483)** — limity/guardraile (D10) + streaming raport CSV. **MERGED (PR #1548), #1483 zamknięte z live proof.** Tenant.importMaxRows/FileSize (422), ImportProfile.allowedErrorsPct (abort→Failed), rate-limiter import_trigger (429), ImportReportCsvController StreamedResponse + iterateBySession, php.ini D10 100M.
  - [x] **IMP2-2.8 (#1484)** — security plików. **MERGED (PR #1549), #1484 zamknięte z live proof** (zip-bomb 422, folder /etc 422, Caddy 413). XlsxArchiveGuard (central-dir statIndex), FolderPathGuard (containment IMPORT_SOURCE_BASE_PATH), CsvStreamWriter sanitizer (export), Caddy request_body 150MB.
  - [x] **IMP2-2.9 (#1485)** — równoległość/locki (ADR-0019 D11). **MERGED (PR #1550, squash `6ae23bfb`), #1485 zamknięte z live-proof 409.** Per-tenant `BulkOperationLock` w 3 entry-pointach (BulkEdit/BulkActions/BulkSessions::rollback) → 409 RFC7807; 12 handlerów `Bulk/*` w `docs/architecture/concurrency-matrix.md` (covered-by-caller). Per-id `OptimisticLockException` retry w `RebuildAttributesIndexedHandler` (**`resetManager()` nie `clear()`** — flush-konflikt zamyka EM; lekcja w lessons). Messenger `retry_strategy` (5×, 30s→300s) + `ImportRunDeadLetterListener` (exhausted → ImportSession `failed`). Live smoke: bulk-edit + bulk-actions 409 podczas locka (A=202/5000).
  - [x] **IMP2-2.10 (#1486)** — pre-import backup spięty z sesją (D11). **MERGED (PR #1551, squash `90904ef3`), #1486 zamknięte z live-proof 422/404/200.** `StartImportController` waliduje `do_backup`+`backup_id` (422 bez-id / 404 unknown+foreign / 422 not-completed / link), reuse `BackupVoter` READ (authz gap z review naprawiony). `Get`/`List` serializują `backup:{id,status,started_at}|null` (bez migracji — pole już zmapowane). FE: checkbox→`onBackupCreated`→`StepConfirm` `backup_id`→ImportShowPage sekcja + i18n. `framework.yaml`+matryca: backup_trigger 1/h kolizja z 4.1 udokumentowana. Testy: ApiTestCase 20/20 (6 nowych). Live smoke: 422/404/200 (+ GET backup) na pim.localhost.

### 🏁 ETAP 2 IMP2 KOMPLETNY (10/10) — #1477–#1486 wszystkie merged + zamknięte z proof
Streaming readers (2.1) · staged upload (2.2) · pauza/wznów/checkpoint (2.3) · undo-log+rollback v2 (2.4) · import_logs RLS+GUC (2.5) · perf bulk-path+diagnoza pamięci (2.6) · limity/guardraile (2.7) · security plików (2.8) · równoległość/locki (2.9) · pre-import backup (2.10).

## Audyt końcowy IMP2 + follow-upy (2026-06-16)
- **Audyt epiku IMP2** (workflow multi-agentowy, 27 ticketów 0.1–2.10): raport `Project Plan/audyt/audyt-imp2-2026-06-16.md` (14 ✅ / 12 ⚠️ / 1 ❌), karta prawdy odświeżona. Flagowy bug 2.8 (XLSX formula injection) potwierdzony empirycznie (`<f>HYPERLINK(...)</f>`).
- **9 follow-up issues (#1552–#1560) → 10 PR, wszystkie MERGED + issues CLOSED:**
  - #1552 P0 XLSX formula injection — `neutraliseFormula` w `XlsxStreamWriter` (PR #1561)
  - #1553 P1 KPI „pominięte" → `skipped_count` w UI (PR #1562)
  - #1554 P1 `allowedErrorsPct` konfigurowalny przez API + v0.json (PR #1564)
  - #1555 P1 pola `zip_file_name`/`zip_file_size_bytes` w kontrakcie sesji (PR #1563)
  - #1556 P2 testy kanonu JSONB — unit normalise (PR #1566) + D7 migracja + export selecta (PR #1569)
  - #1557 P2 ADR-0019 D6 + linki karty prawdy w #1429/#1430 (PR #1565)
  - #1558 P3 decyzja: `ValueWriteCore` konkretna klasa, nie interface/DTO — YAGNI (PR #1567)
  - #1559 luki testowe 7/7 (2.10/1.3/1.7/1.5/2.3/2.4/2.9, ~18 testów) (PR #1568)
  - #1560 deadlock 40P01 = EM-corruption w abort path; reload session po `em->clear()` (PR #1570)
- **Re-audyt #1559** (workflow fan-out) potwierdził, że luki są realne (audyt trafny — inaczej niż 2.10, które miało 6 przeoczonych testów). **#1560**: deadlock to skutek uboczny `ORMInvalidArgumentException` (save sesji po `em->clear()` z rebuild dispatch), NIE osobny lock-order bug; fix naprawił też pre-existing fail `ImportRunHandlerAsyncTest`.

## Audyt połówkowy przed-SaaS (2026-06-16) — READ-ONLY, branch `audit/2026-06-midpoint`
- **Zakres:** cały projekt, 13 domen (A izolacja multi-tenant → M developer experience) + onboarding, metoda: tool-agenci (PHPStan/Deptrac/semgrep/gitleaks pełna historia/cloc/jscpd/psql) → fan-out subagentów per domena → empiryczna matryca 2-tenant na żywym stacku. Deliverable: **`docs/audit/2026-06/`** (00-executive-summary, 01-findings, 02-domain-reports/A–M, 03-fix-plan, raw/, probes/).
- **Werdykt (a) ścieżka do SaaS: NO-GO.** (b) developer adoption risk: **4/10**.
- **Liczby:** 81 findings — **5 CRITICAL** / 20 HIGH / 38 MEDIUM / 18 LOW.
- **5 CRITICAL:** AUD-001 Mercure anonimowy cross-tenant SSE leak (potwierdzony empirycznie), AUD-007 token_dev_only → account takeover (potwierdzony), AUD-004 Meili filter-key cross-tenant read (potwierdzony), AUD-002 RLS martwy (app=superuser+bypassrls, 0 FORCE — zero defence-in-depth), AUD-005 sekrety w trackowanym `.env` (BYOK master key/JWT).
- **Pozytyw empiryczny:** matryca 2-tenant obaliła podejrzenie wycieku przez Doctrine TenantFilter — izolacja danych domenowych przez REST DZIAŁA nawet dla super-admina (cross-read po ID = 404). NO-GO wynika z wektorów POZA filtrem + braków operacyjnych SaaS (backup martwy 49 dni, offboarding/RODO niewykonalny, indeksy skali usunięte regresją migracji, auth/E2E testy martwe w CI).
- **Status:** artefakty zmergowane (PR #1572, `docs/audit/2026-06/`). Naprawy w toku falami (epik-AUD, milestones `audit-wave-0..3`, issues #1573+).

## Audyt fix-plan — fale napraw (epik-AUD)
- **Wave 0 — KOMPLETNA (7/7, 2026-06-17):** 9 findingów (4 z 5 CRITICAL) naprawione + merged + zamknięte z dowodem **CLOSED MEANS CLOSED** (before/after probe w komentarzu zamknięcia). Wzorzec: failing test → fix → zielona regresja.
  - W0-1 Mercure private+tenant-topics (PR #1619→#1573) · W0-2 Meili filter whitelist (#1618→#1574) · W0-3 platform-operator vs tenant super_admin (#1621→#1575) · W0-4 signed asset preview HMAC/TTL (#1622→#1576) · W0-5 token_dev_only env-gate (#1617→#1577) · W0-6 attr-permission read overlay (#1620→#1578) · W0-7 untrack env secrets + `lint-tracked-secrets` guard (#1623→#1579).
  - Status table + linki: `docs/audit/2026-06/01-findings.md` (sekcja „Status napraw — Wave 0").
- **Wave 1 — KOMPLETNA (13/13, 2026-06-18):** AUD-002 RLS (5. CRITICAL) + 22 findingów HIGH/MEDIUM. **5/5 CRITICAL audytu domknięte.** Wszystkie merged + zamknięte z dowodem CLOSED-MEANS-CLOSED. W1-1 FORCE RLS+pim_app (#1626→#1580) · W1-2 GUC unify (#1625→#1581) · W1-3 break-glass (#1627→#1582) · W1-4 prod-secrets+vault (#1628→#1583) · W1-5 backup-cron (#1630→#1584) · W1-6 MinIO versioning/anti-SPOF (#1637→#1585) · W1-7 offboarding hard-delete+MinIO (#1636→#1586) · W1-8 flush-clear PHPStan rule+mem-limit (#1633→#1587) · W1-9 GIN/GiST indeksy (#1631→#1588) · W1-10 export keyset+batch (#1632→#1589) · W1-11 E2E/auth unblock (#1639→#1590) · W1-12 RBAC/token testy +2 bugfixy (#1634→#1591) · W1-13 onboarding (#1635→#1592). + CVE-2026-54133 jmespath bump (#1629). Status table: `01-findings.md` („Status napraw — Wave 1").
- **Wave 2 — KOMPLETNA (14/14, 2026-06-19):** W2-1..W2-14 (#1593–#1606), 24 findingi HIGH/MEDIUM/LOW. Wszystkie merged + zamknięte z dowodem CLOSED-MEANS-CLOSED. JSONB envelope+additionalProperties (#1641) · HTMLPurifier wysiwyg (#1645) · standard_conforming_strings (#1642) · attributes drift cmd (#1646) · atomic rollback (#1647) · irreversible migrations (#1648) · RFC7807 listener (#1649) · OpenAPI 31→218+ADR-0020 (#1650) · JSON body 413+Dockerfile prod (#1651) · ImageDownload dead-letter+processed_messages self-heal (#1652) · export retention+Scheduler+data_export audit (#1653) · rate-limit reset/invite+worker healthcheck+ErrorBoundary (#1655) · qs/dompurify+guzzle/undici audit-unblock (#1656) · CI paths-ignore+benchmark meta-asercja (#1657). Deferrale: **AUD-052**→#1654 (dh_auditor benchmark-first) · **AUD-054**→ADR-0020 · **AUD-062**→AUD-022 (storageState). Status table: `01-findings.md` („Status napraw — Wave 2").
- **Wave 3 — KOMPLETNA (10/10, 2026-06-20):** #1607–#1616, higiena/dług techniczny. Wszystkie merged + zamknięte z dowodem CLOSED-MEANS-CLOSED. Deptrac Uncovered 5283→0 + Vendor collector + Backup gated + DROP sierot (#1669) · FE jsonFetch ADR-0021+lint + AbstractBulkHandler −627 + product-detail 1499→492 (#1671) · dev-docs vertical-slice (#1666) · dashboard completeness-ring live + mock-banner (#1665) · PermissionResolver scope + grid-virt (#1661) · tenant-deny prod + RBAC meta-test + magic-byte (#1660) · x-powered-by/TRUSTED_HOSTS/import-buffer (#1659) · Meili-degraded-503/chunk/FK-409 (#1663) · CSP-prod-'self'+PermissionRoute (#1664, AUD-075→#1667) · break-glass-runbook+phpstan-hygiene (#1662). Cross-context wzorzec: minimum-viable + enforcement (ADR+lint nie-rosnący) + proof-slice + tracked-deferral. Deferrale: **#1667** (i18n parity/E2E-locale) · **#1668** (Deptrac bulk-286) · **#1670** (FE jsonFetch/monolity). Status table: `01-findings.md` („Status napraw — Wave 3").

## Ostatnie akcje
1. **Audyt fix-plan Wave 3 — KOMPLETNA → CAŁY FIX-PLAN DOMKNIĘTY** (2026-06-20) — 10 ticketów higieny/długu (#1607–#1616), wszystkie merged + zamknięte z dowodem. Cross-context (Deptrac 286-burndown, FE jsonFetch migracja, monolit-split) wzorcem minimum-viable + enforcement + proof-slice + tracked-deferral (#1667/#1668/#1670). Saga #1615: i18n-parity flipuje locale Playwright (mieszane pl/en selektory E2E) → split (CSP+PermissionRoute czyste, i18n→#1667). Race #1614↔#1616 (bg-commit+checkout) odzyskany przez staging-separację. **Razem Wave 0–3 = 44 tickety, 5/5 CRITICAL.**
2. **Audyt fix-plan Wave 2 — KOMPLETNA** (2026-06-19) — 14 ticketów (24 findingi), audit-gate unblock (guzzle/undici CVE) do #1656, regresje rate-limitera naprawione bez wyciszania. Deferrale: AUD-052→#1654, AUD-054→ADR-0020, AUD-062→AUD-022.
3. **Audyt fix-plan Wave 0+1 — KOMPLETNE** (2026-06-17/18) — 20 ticketów, 5/5 CRITICAL domknięte. Bonus: testy W1-12 znalazły+naprawiły 2 bugi HIGH. Follow-up #1638 (24 E2E triage).

## Następny krok — fix-plan KOMPLETNY (epik-AUD); otwarte follow-upy (tracked, nie blokery)
**Cały audyt fix-plan Wave 0–3 domknięty (44 tickety, 5/5 CRITICAL + wszystkie HIGH/MEDIUM/LOW + higiena/dług).** Pierwotne blokery NO-GO zaadresowane (AUD-001/004/007 W0, AUD-002 RLS W1, AUD-003 RBAC). Otwarte follow-upy (dedykowane tickety, NIE blokery — do podjęcia osobno):
- **#1667** — AUD-075 i18n pl/en parity + E2E-locale determinizm (~30 mieszanych selektorów; razem z #1638).
- **#1668** — AUD-053 bulk Deptrac-burndown (~284 skipy Import/Export→Catalog/User; część zamkniętego #1466).
- **#1670** — AUD-055/057 FE bulk: migracja ~136 jsonFetch→useQuery + split 16 monolitów (baseline-lint malejący).
- **#1638** (24 E2E specy triage) · **#1644** (undici/jsdom) · **#1654** (dh_auditor benchmark) · **AUD-062**→AUD-022 (Playwright storageState).
**Decyzja GO/re-audit** → Phase 7 (manual red-team Marcina 15-point + optional external pentest) + `00-executive-summary.md`. Każdy follow-up: own branch+PR+CI+merge, security→failing-test-first, close→before/after probe. Operator: ultracode ON, komunikacja+rozumowanie po polsku.

> Kontekst IMP2 (zamknięty): ETAP 0–2 (#1460–#1486) + audyt IMP2 + 9 follow-upów (#1552–#1560). Etap 3+ IMP2 (#1487–#1498) odłożony — priorytet to fix-plan przed-SaaS.

### Świadome odejścia etapu-2 (marsz #1483→#1486)
- **2.10 pozytyw smoke**: dev pgBackRest zwraca `failed` natychmiast (cron stale) → completed backup zaseedowany w DB dla live-proof; pełny e2e snapshot zależy od działającego pgBackRest.
- **2.10 authz**: dodany `BackupVoter` READ w StartImport (gap z adversarial review) — odejście od „tylko `imports:run`", spójność z `GetBackupController`.
- **2.9 retry**: `OptimisticLockException` zamyka EM → `resetManager()` zamiast `clear()` (realny bug, nie tylko test).
Env quirki: po zmianie encji/migracji/config `docker compose restart api worker`. **Testy Api/Import lokalnie**: `-e MESSENGER_IMPORT_TRANSPORT_DSN=sync://` + `-e APP_DEBUG=0` ORAZ `cache:clear --env=test` z `APP_DEBUG=0` (osobny compiled cache!). CI: świeży kontener.

## Aktywne blokery
- Brak (IMP2-1.8 pozostałe items to praca, nie bloker).

## Stałe przypomnienia (workflow IMP2)
- Testy `Api/*`: `cache:clear --env=test` + `docker compose exec -T -e APP_ENV=test api php vendor/bin/phpunit ...` (inaczej `test.service_container` not found).
- PHPStan: `cache:warmup --env=dev` + `composer phpstan -- --memory-limit=1G`.
- Deptrac: Import/Export sięgają Channel TYLKO przez `Channel\Contracts` (port `ScopeEnumeratorInterface`).
- `apps/api/config/reference.php` auto-regeneruje się dirty (środowiskowy szum) — nie commitować.
- Przed `gh issue close`: live-stack smoke na pim.localhost z dowodem (CLOSED MEANS CLOSED).
