/**
 * Build-time constants injected by Vite `define` (see vite.config.ts /
 * vitest.config.ts). `__APP_VERSION__` carries the app version from
 * package.json (or the APP_VERSION env override in CI builds).
 */
declare const __APP_VERSION__: string;
