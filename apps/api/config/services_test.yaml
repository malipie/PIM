# AUD-DATA-001 (#3020) — test-only SQL shape counter. The production
# QueryTimingMiddleware deliberately aggregates durations without retaining SQL.
services:
    App\Tests\Support\SqlQueryCounter:
        public: true

    pim.test.sql_logging_middleware:
        class: Doctrine\DBAL\Logging\Middleware
        arguments:
            - '@App\Tests\Support\SqlQueryCounter'
        tags:
            - { name: doctrine.middleware }

    # TEMPORARY (#3053) — public alias so the rollback probe can call the
    # rebuilder directly and tell delivery apart from computation.
    test.catalog.attributes_indexed_rebuilder:
        alias: App\Catalog\Application\AttributesIndexedRebuilder
        public: true
