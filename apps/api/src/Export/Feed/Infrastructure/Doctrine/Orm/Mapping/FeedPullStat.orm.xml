<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Export\Feed\Domain\Entity\FeedPullStat"
            table="feed_pull_stats">

        <unique-constraints>
            <!-- The UPSERT conflict target — one counter row per feed per hour. -->
            <unique-constraint name="feed_pull_stats_bucket_uniq" columns="tenant_id,feed_id,bucket_start"/>
        </unique-constraints>

        <id name="id" type="uuid" column="id">
            <generator strategy="NONE"/>
        </id>

        <many-to-one field="tenant" target-entity="App\Shared\Domain\Tenant">
            <join-column name="tenant_id" referenced-column-name="id" nullable="false" on-delete="RESTRICT"/>
        </many-to-one>

        <field name="feedId" type="uuid" column="feed_id"/>
        <field name="bucketStart" type="datetime_immutable" column="bucket_start"/>
        <field name="pullCount" type="integer" column="pull_count">
            <options>
                <option name="default">0</option>
            </options>
        </field>

    </entity>

</doctrine-mapping>
