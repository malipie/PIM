{
  "name": "@pim/docs",
  "version": "1.8.7",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "dev": "pnpm sync && vitepress dev content",
    "build": "pnpm sync && vitepress build content",
    "preview": "vitepress preview content"
  },
  "devDependencies": {
    "@scalar/api-reference": "^1.67.0",
    "vitepress": "^1.6.0"
  }
}
